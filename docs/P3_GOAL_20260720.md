# P3 阶段 Goal：架构革命 + SOTA 真实对齐 + NO-GO 修复 → 顶刊冲刺

## 目标 (Objective)

在远程服务器 `cunyuliu@36.137.135.49:22` 工作目录 `/home/cunyuliu/pc_cng_research` 执行 P3-XX 任务（详见 `docs/00_当前有效文档/顶刊论文核心思想与从0到1落地方案.md` Section 30），通过**架构革命 + SOTA 真实对齐 + NO-GO 任务翻盘**，将项目从当前 v3 九维评分 5.2/10（J. Chem. Inf. Model. 梯队）提升至 8.0+/10（Chem. Sci. / Nat. Mach. Intell. 梯队）。

**最终交付**：`docs/manuscript_v3_YYYYMMDD.md` + `docs/manuscript_supplementary_v3_YYYYMMDD.md` + `docs/cover_letter_v3_YYYYMMDD.md`，并满足 Chem. Sci. / Nat. Mach. Intell. 投稿标准。

## 硬约束（不可违反）

1. **运行时 PID 发现**：不擅自终止任何运行中进程；GPU 4 不使用（calibrate PID 2544995）；P2-05 (PID 3002373) / P2-07 (PID 149448) 等历史进程的 PID 必须 runtime rediscovered，若已完成则收割结果，未完成则并行启动新任务不抢占其资源。
2. **不修改 `docs/00_当前有效文档/` 下既有 `.md` 文件**（仅追加新文档；本 goal 文档例外，已由用户授权更新）。
3. **禁止删除或重命名已有 `results/` 子目录**。
4. **所有新代码必须配套单元测试**（覆盖率 ≥ 80%）。
5. **所有性能主张必须基于 10-seed paired significance test**（family-cluster bootstrap CI），不再用 decoder seeds 替代 training seeds。
6. **AiZynthFinder / DFT / SOTA 对比工具必须在独立 venv 安装**（避免污染 `pc_cng` 环境）。
7. **不修改 v1 frozen namespace** (`data/reconstructed/p0_data_reconstruction_v1/`)。
8. **不修改已完成 v2 审计结果**；新增审计以 v3 命名。
9. **所有新增训练必须接入 `--train-idx/--val-idx/--test-idx` 强制合同**。
10. **任何 "improves TE/stability/expression" 必须加 predicted/internal proxy 限定词**，直到 P2-01 multi-region oracle 验证完成（本项目是化学反应预测，此条参考执行，对应"improves yield/selectivity"）。
11. **不修改 `git@github.com:Cunyu-Liu/PC_CNG.git` 已 push 的历史 commit**；新代码以新 commit 追加。

## 任务优先级与执行顺序

### P3-00：收尾 P2-05 / P2-07 + 重新生成 manuscript v2.1（必须最先执行）

**输入**：
- P2-05 (PID 3002373) 已运行 `uspto_to_ord` 7/10 seeds（截至 2026-07-20 17:00）
- P2-07 (PID 149448) Transformer training 已运行 3+ 小时无 stdout

**操作**：
1. Runtime rediscover PID 3002373 / 149448：`ps -p <PID>` 检查是否存活
2. 若已完成：收割结果到 `results/cross_dataset_transfer_v2_20260720/` 和 `results/transformer_negative_generator_20260720/`
3. 若未完成：再等待最多 2 小时；超时则 kill 并以 smoke results 记录
4. 重新生成 `docs/manuscript_v2_1_YYYYMMDD.md`（基于最新 P2-05 / P2-07 结果）

**验收命令**：
```bash
ps -p 3002373 149448 2>/dev/null
ls -la results/cross_dataset_transfer_v2_20260720/uspto_to_ord/ | wc -l
ls -la results/transformer_negative_generator_20260720/
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.build_manuscript_v2 \
  --version v2.1 --out docs/manuscript_v2_1_$(date +%Y%m%d).md
```

**Go/No-Go**：必须生成 `manuscript_v2_1_*.md`（不论 P2-05/P2-07 是否完成）；P2-05 若完成且 CI 显著 >0 → 直接进入 P3-03 的备选方案；P2-07 若完成且 MRR 改善 → 进入 P3-01 的基线对照。

---

### P3-01：架构革命 — 预训练 Transformer backbone + adapter tuning（最高优先级，核心壁垒）

**痛点**：v3 九维评估"模型架构"维度仅 2/10（最差），当前仍是 2018 年的 GNN/MLP 架构，与 Chem. Sci. / Nat. Mach. Intell. 梯队的 E2E Transformer 范式严重脱节。

**输入**：
- 当前 backbone：`chem_negative_sampling/models/backbone.py`（GNN + MLP）
- 候选预训练 backbone：ChemBERTa-2 / MoLFormer / Chemformer（任选 ≥1 个，建议 Chemformer 因其反应建模能力）
- adapter 实现：LoRA / prefix-tuning / adapter-tuning

**操作**：
1. 在 `chem_negative_sampling/models/pretrained_backbone.py` 实现预训练 backbone 加载
2. 在 `chem_negative_sampling/models/adapter.py` 实现 adapter tuning（建议 LoRA r=8）
3. 训练脚本：`chem_negative_sampling/training/train_pretrained.py`（必须接入 `--train-idx/--val-idx/--test-idx`）
4. 10-seed paired significance test：与当前 GNN baseline 对比

**验收命令**：
```bash
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pytest \
  chem_negative_sampling/tests/test_pretrained_backbone.py \
  chem_negative_sampling/tests/test_adapter.py -v

PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.training.train_pretrained \
  --backbone chemformer --adapter lora_r8 \
  --train-idx data/processed/train_idx_v3.json \
  --val-idx data/processed/val_idx_v3.json \
  --test-idx data/processed/test_idx_v3.json \
  --seeds 10
```

**Go/No-Go**：
- **GO**：10-seed paired bootstrap CI 下限 >0，且相比 GNN baseline MRR 提升 ≥5pp
- **NO-Go 退路**：若 Chemformer 不可用，退回 ChemBERTa-2 + LoRA；若仍失败，记录 L15（架构革命失败）并继续 P3-02 起的其他任务

**产出**：
- `results/pretrained_backbone_chemformer_lora_YYYYMMDD/`（含 10-seed metrics.json + summary.md）
- `chem_negative_sampling/models/pretrained_backbone.py`
- `chem_negative_sampling/models/adapter.py`
- `chem_negative_sampling/training/train_pretrained.py`

---

### P3-02：SOTA 真实对齐 — conda-pack 离线安装（修复 P2-06 NO-GO）

**痛点**：P2-06 因服务器无外网无法 pip install LocalRetro / Graph2SMILES 等 SOTA，被迫用 tanimoto_nn 作 baseline，被审稿人质疑 SOTA 选择不公平。

**输入**：
- 当前 SOTA baseline 仅 tanimoto_nn（P2-06 NO-GO 原因）
- 网络受限服务器 `36.137.135.49`

**操作**：
1. 在本地 Mac（有外网）创建 conda env：`conda create -n sota_offline python=3.10`
2. 在该 env 中 pip install LocalRetro / Graph2SMILES / ReactionT5 / Chemformer-ranker
3. `conda pack -n sota_offline -o sota_offline.tar.gz`
4. `scp sota_offline.tar.gz cunyuliu@36.137.135.49:/home/cunyuliu/pc_cng_research/envs/`
5. 服务器解压：`mkdir -p envs/sota_offline && tar -xzf envs/sota_offline.tar.gz -C envs/sota_offline`
6. 在 USPTO-MIT-50k 上跑 10-seed 对比

**验收命令**：
```bash
ls -la envs/sota_offline/bin/python
envs/sota_offline/bin/python -c "import localretro; import graph2smiles; print('SOTA ready')"
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  envs/sota_offline/bin/python -m pc_cng.evaluation.sota_comparison \
  --dataset uspto_mit_50k --seeds 10
```

**Go/No-Go**：
- **GO**：PC-CNG 相比每个 SOTA 至少打平（CI 下限 > -1pp），且至少对 2 个 SOTA 显著更优（CI 下限 > 0）
- **NO-Go 退路**：若 SOTA 仍跑不通，记录 L16（SOTA 离线安装失败），在 manuscript 中以 "我们对比了公开 leaderboard 上汇报的 SOTA 数字" 替代直接复现

**产出**：
- `envs/sota_offline.tar.gz`（不 push 到 GitHub，加 .gitignore）
- `results/sota_comparison_v2_uspto_mit_50k_YYYYMMDD/`

---

### P3-03：跨数据集迁移机制 — fine-tuning head（修复 P2-05 NO-GO）

**痛点**：P2-05 直接 transfer 导致 0/7 pairs CI positive。正确做法应该是 **frozen backbone + lightweight fine-tuning head**，而非直接复用 source dataset 的 head。

**输入**：
- P2-05 结果：`results/cross_dataset_transfer_v2_20260720/`
- 7 个迁移对：uspto→ord / ord→uspto / hitea→uspto / hitea→ord / uspto→hitea / ord→hitea / uspto_openmolecules→ord

**操作**：
1. 实现 `chem_negative_sampling/training/finetune_head.py`：冻结 backbone，仅训练 1 层 MLP head（≤100K 参数）
2. 每个迁移对用 10% target data 做 few-shot fine-tuning，10-seed 评估
3. 对比 3 个变体：(a) direct transfer / (b) fine-tune head / (c) full fine-tune

**验收命令**：
```bash
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.training.finetune_head \
  --source uspto --target ord \
  --variants direct,head_finetune,full_finetune \
  --seeds 10
```

**Go/No-Go**：
- **GO**：fine-tune head 在 ≥5/7 pairs 上 CI 下限 > 0（相比 direct transfer 提升 ≥3pp）
- **NO-Go 退路**：若仍失败，记录 L17（迁移机制失败），在 manuscript 中讨论"PC-CNG 学到的 representation 具有部分但非完全的跨数据集泛化能力"

**产出**：
- `results/cross_dataset_finetune_head_YYYYMMDD/`
- `chem_negative_sampling/training/finetune_head.py`

---

### P3-04：真实条件预测 — USPTO 500k + catalyst/solvent 分层（修复 P2-08 NO-GO）

**痛点**：P2-08 用合成 condition data 导致 -2.50pp；真实条件预测需要真实数据。

**输入**：
- USPTO 500k reaction dataset（公开）
- Doyle/MacMillan HTE 数据（高精度条件标签）

**操作**：
1. 下载 USPTO 500k：`scripts/download_uspto_500k.sh`
2. 实现 condition extraction：`chem_negative_sampling/data/extract_conditions.py`（catalyst / solvent / reagent / temperature）
3. 训练 condition predictor：`chem_negative_sampling/training/train_condition.py`
4. 10-seed 对比 P2-08 的合成数据 baseline

**验收命令**：
```bash
ls -la data/raw/uspto_500k_reactions.csv
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.training.train_condition \
  --data data/processed/uspto_500k_conditions.json \
  --seeds 10
```

**Go/No-Go**：
- **GO**：真实数据下 condition Top-1 accuracy ≥ 70%（相比 P2-08 baseline 提升 ≥20pp）
- **NO-Go 退路**：记录 L18（真实条件预测仍困难），在 manuscript 中讨论"condition prediction 的瓶颈在于数据稀疏性而非模型容量"

**产出**：
- `results/condition_prediction_v2_uspto_500k_YYYYMMDD/`
- `data/processed/uspto_500k_conditions.json`

---

### P3-05：HTE 数据集成 — Doyle/MacMillan 高精度实验数据

**痛点**：v3 九维评估"数据集全面性"维度仅 5/10，缺乏高精度实验数据。

**输入**：
- Doyle et al. Nature 2019 HTE data (DFT-optimized)
- MacMillan et al. Science 2022 photoredox HTE data
- Perera et al. Science 2020 C-H functionalization HTE

**操作**：
1. 实现 `chem_negative_sampling/data/hte_loader.py`：自动解析 HTE CSV
2. 在 HTE 上做 leave-one-out 评估（小数据集，不需要 10-seed）
3. 对比 PC-CNG vs random negative vs no negative

**验收命令**：
```bash
ls -la data/raw/hte_doyle_2019.csv data/raw/hte_macmillan_2022.csv
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.evaluation.hte_eval \
  --datasets doyle_2019,macmillan_2022,perera_2020
```

**Go/No-Go**：
- **GO**：PC-CNG 在 ≥2/3 HTE datasets 上 Top-1 accuracy > random baseline by ≥5pp
- **NO-Go 退路**：若 HTE 数据太小不可靠，记录 L19 并在 supplementary 中报告

**产出**：
- `results/hte_evaluation_YYYYMMDD/`
- `data/raw/hte_*.csv`

---

### P3-06：多任务联合训练 — retrosynthesis + condition + yield

**痛点**：当前各下游任务独立训练，未利用任务间共享表示。

**输入**：
- P3-01 backbone（共享 encoder）
- 3 个 task head：retrosynthesis / condition / yield

**操作**：
1. 实现 `chem_negative_sampling/models/multitask.py`：shared encoder + 3 task heads
2. 训练：alternating minibatch + uncertainty weighting (Kendall et al. 2018)
3. 10-seed 对比单任务 baseline

**验收命令**：
```bash
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.training.train_multitask \
  --tasks retro,condition,yield --seeds 10
```

**Go/No-Go**：
- **GO**：多任务相比单任务在至少 2/3 任务上不显著变差（CI 下限 > -2pp），且至少 1/3 任务显著变好
- **NO-Go 退路**：若多任务负迁移，记录 L20 并回退单任务

**产出**：
- `results/multitask_joint_training_YYYYMMDD/`
- `chem_negative_sampling/models/multitask.py`

---

### P3-07：LLM-as-judge 专家代理审查（修复 P2-03 DEFERRED）

**痛点**：P2-03 expert review 因找不到真人化学专家 DEFERRED。可用 LLM（GPT-4 / Claude / Gemini）作化学专家代理。

**输入**：
- 100 个 PC-CNG 生成的 negative reactions（分层抽样：easy/medium/hard）
- 3 个 LLM：GPT-4 / Claude-3.5-Sonnet / Gemini-1.5-Pro

**操作**：
1. 实现 `chem_negative_sampling/evaluation/llm_judge.py`：构造 prompt → 调 LLM API → 解析评分
2. 每个 reaction 由 3 个 LLM 独立评分（majority vote + inter-LLM agreement 评估）
3. 与 DFT 结果（P2-02）做相关性分析，验证 LLM-as-judge 的可信度

**验收命令**：
```bash
ls -la results/llm_judge_YYYYMMDD/judgments.json
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.evaluation.llm_judge_analysis \
  --judgments results/llm_judge_YYYYMMDD/judgments.json \
  --dft results/dft_validation_chemoselectivity_20260720/
```

**Go/No-Go**：
- **GO**：LLM 间 agreement (Cohen's kappa) ≥ 0.6，且 LLM 评分与 DFT 一致率 ≥ 80%
- **NO-Go 退路**：若 LLM 不可靠，记录 L21 并在 manuscript limitation 中讨论

**产出**：
- `results/llm_judge_YYYYMMDD/`
- `chem_negative_sampling/evaluation/llm_judge.py`

---

### P3-08：综合 benchmark 套件 — 6 个维度全覆盖

**痛点**：当前评估维度不全（v3 评估"测评全面性"5/10）。

**操作**：实现统一 benchmark 套件，覆盖 6 个维度：
1. Negative generation quality (validity / uniqueness / diversity)
2. Downstream task improvement (retrosynthesis / condition / yield)
3. Cross-dataset generalization (transfer)
4. Computational efficiency (latency / throughput)
5. Chemical plausibility (DFT / LLM-judge)
6. Ablation studies (PC-CNG components)

**验收命令**：
```bash
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pc_cng.evaluation.benchmark_suite \
  --dimensions all --output results/benchmark_suite_v3_YYYYMMDD/
```

**Go/No-Go**：
- **GO**：6 个维度全部产出 metrics.json + report.md
- **NO-Go 退路**：无（必须完成）

**产出**：
- `results/benchmark_suite_v3_YYYYMMDD/`
- `chem_negative_sampling/evaluation/benchmark_suite.py`

---

### P3-09：Manuscript v3 + 顶刊投稿包

**输入**：P3-01 ~ P3-08 全部结果

**操作**：
1. 重新生成 `docs/manuscript_v3_YYYYMMDD.md`（基于 P3 全部结果）
2. 重新生成 `docs/manuscript_supplementary_v3_YYYYMMDD.md`
3. 重新生成 `docs/cover_letter_v3_YYYYMMDD.md`（目标 Chem. Sci. / Nat. Mach. Intell.）
4. 生成 `docs/target_journal_decision_v3_YYYYMMDD.md`：基于 v3 九维评分决定最终目标期刊
5. 跑全量单元测试：`pytest chem_negative_sampling/tests/ -v`

**验收命令**：
```bash
ls -la docs/manuscript_v3_*.md docs/manuscript_supplementary_v3_*.md docs/cover_letter_v3_*.md
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pytest chem_negative_sampling/tests/ -v 2>&1 | tail -10
```

**Go/No-Go**：
- **GO**：v3 九维评分 ≥ 8.0/10，且 manuscript v3 长度 ≥ 25KB，单元测试 100% 通过
- **NO-Go**：记录未达标维度，生成 manuscript v3 但降级目标期刊至 J. Chem. Inf. Model. / Digital Discovery

**产出**：
- `docs/manuscript_v3_YYYYMMDD.md`
- `docs/manuscript_supplementary_v3_YYYYMMDD.md`
- `docs/cover_letter_v3_YYYYMMDD.md`
- `docs/target_journal_decision_v3_YYYYMMDD.md`

---

## 执行策略

1. **P3-00 先行**（必须最先执行）：收割 P2-05/P2-07 结果，生成 manuscript v2.1
2. **P3-01 最高优先级**（架构革命是核心壁垒，决定能否冲顶刊）
3. **P3-02 与 P3-01 并行**（conda-pack 在本地 Mac 操作，不占服务器 GPU）
4. **P3-03 依赖 P3-01**（需要 P3-01 的 backbone 做 fine-tuning head）
5. **P3-04 与 P3-05 并行**（数据准备，不占 GPU）
6. **P3-06 依赖 P3-01 + P3-04 + P3-05**（多任务需要 backbone + 多个数据集）
7. **P3-07 独立**（LLM API 调用，不占 GPU）
8. **P3-08 依赖 P3-01 ~ P3-07 全部完成**
9. **P3-09 最后执行**（聚合所有结果）

## NO-GO 翻盘策略

| 原 NO-GO | 翻盘任务 | 新方法 |
|---------|---------|--------|
| P2-05 cross-dataset transfer | P3-03 | fine-tuning head 替代 direct transfer |
| P2-06 SOTA comparison | P3-02 | conda-pack 离线安装真实 SOTA |
| P2-07 transformer smoke | P3-01 | 预训练 backbone + adapter 替代 from-scratch |
| P2-08 condition prediction | P3-04 | 真实 USPTO 500k 数据替代合成数据 |
| P2-03 expert review (DEFERRED) | P3-07 | LLM-as-judge 替代真人专家 |

## 资源约束

- **GPU**：可用 GPU 0/1/2/3（GPU 4 禁用，calibrate PID 2544995）；P3-01 训练预估需 1 张 GPU × 24h × 10 seeds = 240 GPU-hours
- **存储**：USPTO 500k 约 2GB；预训练 backbone 约 500MB；HTE 数据约 10MB
- **LLM API**：P3-07 预估 100 reactions × 3 LLMs × $0.05/call = $15

## 验收命令汇总

```bash
# 1. P2-05/P2-07 收割
ps -p 3002373 149448 2>/dev/null
ls results/cross_dataset_transfer_v2_20260720/uspto_to_ord/ | wc -l
ls results/transformer_negative_generator_20260720/

# 2. P3 各任务产出
ls -d results/pretrained_backbone_*_YYYYMMDD \
       results/sota_comparison_v2_*_YYYYMMDD \
       results/cross_dataset_finetune_head_YYYYMMDD \
       results/condition_prediction_v2_uspto_500k_YYYYMMDD \
       results/hte_evaluation_YYYYMMDD \
       results/multitask_joint_training_YYYYMMDD \
       results/llm_judge_YYYYMMDD \
       results/benchmark_suite_v3_YYYYMMDD 2>/dev/null

# 3. Manuscript v3
ls -la docs/manuscript_v3_*.md docs/manuscript_supplementary_v3_*.md docs/cover_letter_v3_*.md

# 4. 单元测试
PYTHONPATH=/home/cunyuliu/pc_cng_research/chem_negative_sampling \
  /home/cunyuliu/miniconda3/envs/pc_cng_gpu/bin/python -m pytest chem_negative_sampling/tests/ -v 2>&1 | tail -10

# 5. Git push 新代码
cd /home/cunyuliu/pc_cng_research && git status && git log --oneline -5
git add . && git commit -m "P3: architecture revolution + SOTA + NO-GO fixes" && git push origin main
```

## 成功标准

- **必达**：v3 九维评分 ≥ 7.0/10，manuscript v3 生成，单元测试 100% 通过
- **目标**：v3 九维评分 ≥ 8.0/10，至少 3 个原 NO-GO 翻盘为 GO
- **冲刺**：v3 九维评分 ≥ 9.0/10，全部 NO-GO 翻盘，目标期刊升级至 Nat. Mach. Intell.

## 注意事项

1. **不擅自终止任何运行中进程**：所有 PID 必须 runtime rediscovered
2. **不修改 GitHub 已 push 历史**：新代码以新 commit 追加，不 amend / rebase
3. **每个 P3 任务完成后立即 commit + push**：便于版本追溯
4. **所有结果目录带日期后缀**：不与 P2 的 `results/*_20260720/` 冲突
5. **遇到 L15-L21 新限制时如实记录**：在 manuscript limitation 中讨论，不掩盖

---

## 启动命令（直接复制到 Trae 执行）

```
/goal 连接 ssh cunyuliu@36.137.135.49 -p 22，工作目录 /home/cunyuliu/pc_cng_research。执行 P3-XX 任务（详见 docs/00_当前有效文档/顶刊论文核心思想与从0到1落地方案.md Section 30，goal 详情见 docs/P3_GOAL_20260720.md）。约束：[11 条硬约束见上]。预期产出：[P3-00 ~ P3-09 各任务产出]。验收命令：[见验收命令汇总]。执行顺序：P3-00 → P3-01 (并行 P3-02) → P3-03 (依赖 P3-01) → P3-04/P3-05 (并行) → P3-06 (依赖 P3-01/04/05) → P3-07 (独立) → P3-08 (依赖全部) → P3-09 (最后)。NO-GO 翻盘策略：P2-05→P3-03 fine-tune head / P2-06→P3-02 conda-pack / P2-07→P3-01 预训练 backbone / P2-08→P3-04 真实 USPTO 500k / P2-03→P3-07 LLM-as-judge。成功标准：v3 九维评分 ≥8.0/10 + manuscript v3 + 全部 NO-GO 翻盘 + 100% 单元测试通过。每个任务完成后立即 commit + push 到 git@github.com:Cunyu-Liu/PC_CNG.git。
```
