# 顶刊论文核心思想与从0到1落地方案

## 0. 文档定位

本文件用于把项目凝练成一个可执行、可审稿、可复现的科研方案。核心原则是：

- 不把模型生成结果伪装成真实实验失败结果。
- 不声称未经验证的反应一定不可行。
- 所有合成负样本均标注为 `synthetic counterfactual negative reactions`。
- 任何性能提升都必须通过公开数据、严格切分、下游任务和消融实验证明。
- 若冲击顶刊，必须尽可能加入真实 HTE 数据、专家审查或小规模实验验证。

### 0.1 本轮执行更新：模型设计优先

后续执行将把项目重心从“构建一个负样本数据集”转为“提出一个底层反应图反事实生成模型”。数据集只是模型能力的外显产物，论文主贡献应是模型如何学习反应失败边界，并如何在逆合成任务中提供可验证收益。

新的执行主线为：

1. 先实现一个最小可运行的反应中心反事实编辑器，跑通生成、验证、打分、导出。
2. 用生成负样本训练或评估一个轻量级反应可行性/逆合成排序模型，证明下游 utility。
3. 再把规则编辑器升级为可学习的 graph edit decoder 和 failure latent space。
4. 最终用全量 USPTO/ORD/HTE 数据训练大模型，并做 OOD、消融和专家/实验验证。

判断模型是否有作用的最低证据不是“生成了很多负样本”，而是：

> 在严格 baseline 和 OOD 验证下，由该模型生成的合成负样本能比随机负样本、规则负样本和模板扰动负样本更稳定地提升逆合成候选排序、反应可行性判断或反应预测模型。

## 1. 一句话核心思想

真实失败反应数据能显著提升化学反应模型，但真实负样本稀缺、不可机读且实验获取昂贵；本项目拟提出一种以反应中心图编辑为核心的物理化学约束反事实生成模型，用少量真实负样本校准失败潜空间，用大量成功反应学习化学语法，并通过 semi-hard 课程学习生成靠近反应可行性边界的合成负样本，从而提升低数据场景下的反应预测、逆合成排序和奖励模型训练。

## 2. 科学问题与研究空白

### 2.1 已知事实

Science Advances 2025 论文 *Negative chemical data boosts language models in reaction outcome prediction* 表明，负反应数据可以通过 RL feedback 显著改善反应结果预测，尤其是在正样本很少时更明显。该论文的核心价值是证明了“负样本有用”。

但它同时暴露出更上游的问题：

1. 高质量负反应数据非常少。
2. 大量失败实验存在于实验室记录、HTE 筛选、公司 ELN 中，但通常不可公开、不可机读、难以标准化。
3. 公开反应数据库主要偏向成功反应，存在严重 reporting bias。
4. 随机拼接反应物和产物得到的负样本过于简单，无法刻画反应可行性边界。

### 2.2 本项目要解决的问题

本项目不直接回答“负样本是否有用”，而是回答：

> 当真实负样本严重不足时，能否通过创新模型架构生成物理化学合理、靠近反应可行性边界、能真实提升下游模型表现的合成负反应数据？

### 2.3 关键难点

这个问题困难的根源不在生成本身，而在“可信负样本”的定义。

一个未报道反应并不等于不可行反应。模型生成的负样本也可能是潜在新反应。因此必须解决：

- false negative 风险：避免把潜在可行反应误标为负样本。
- trivial negative 风险：避免生成太简单、对训练没有价值的负样本。
- chemical validity 风险：避免生成无效 SMILES、价态错误、原子不守恒的伪化学样本。
- distribution leakage 风险：避免训练和测试反应族、底物、产物或模板泄漏。
- evaluation inflation 风险：避免只在模型自建负样本上自证有效。

## 3. 顶刊级论文主张

建议论文主张凝练为：

> We introduce a physicochemically constrained counterfactual generator that learns failure directions from scarce real negative reactions and chemical grammar from abundant positive reactions. By generating semi-hard synthetic negative reactions near the feasibility boundary, the framework improves reaction prediction and retrosynthesis ranking in low-data regimes while explicitly controlling false-negative risk.

中文表述：

> 我们提出一个物理化学约束的反事实负反应生成器。它从少量真实失败反应中学习失败方向，从大量成功反应中学习化学语法，并在反应可行性边界附近生成 semi-hard 合成负样本，从而在低数据条件下提升反应预测与逆合成排序，同时显式控制假阴性风险。

## 4. 拟解决的核心科学假设

### H1：少量真实负样本足以校准失败方向

假设：即使只有几十到几百条真实失败反应，也能通过原型学习、adapter tuning 或偏好优化学到不同失败类型的方向，例如区域选择性错误、化学选择性错误、条件失败和低转化。

验证：比较无真实负样本、有 20/50/100/500 条真实负样本时，生成负样本的失败类型可控性、semi-hard 占比和下游收益。

### H2：反应中心编辑优于从零生成

假设：从成功反应出发进行局部反事实编辑，比从零生成完整 reaction SMILES 更容易得到化学合法、与正样本相近、对模型困难的负样本。

验证：比较 raw SMILES generator、Transformer-VAE、graph edit generator 和本项目的 constrained reaction-center editor。

### H3：semi-hard 课程学习优于 top-k hardest mining

假设：最难负样本往往假阴性风险高或导致训练不稳定；位于中等困难区间的 semi-hard negatives 更稳定、更有训练价值。

验证：比较 random negatives、easy negatives、top-k hardest negatives、semi-hard curriculum negatives 的下游效果和训练稳定性。

### H4：合成负样本能提升真实任务，而不仅是提升自建分类任务

假设：高质量合成负样本可以提升真实反应结果预测、低数据反应族泛化、逆合成候选排序和奖励模型训练，而不是只提升“区分我自己生成的负样本”的指标。

验证：必须在真实正样本测试集、真实 HTE 负样本测试集、OOD 反应族和外部模型上进行评估。

### H5：逆合成排序提升可以作为模型有效性的核心外部证据

假设：如果一个负样本生成模型确实学到了反应失败边界，那么它生成的负样本应能帮助逆合成系统识别不可行候选前体、降低 hallucinated route 排名，并提升真实可行路线的排序质量。

验证：构造或使用包含一个正路线和多个负路线候选的 retrosynthesis reranking benchmark，对比不同负样本来源训练出的 reranker 在 MRR、NDCG、top-k route recall、false-positive route rate 上的表现。

## 5. 模型总体架构：PC-CNG

建议模型名：

**PC-CNG: PhysChem-Constrained Counterfactual Negative Generator**

中文名：

**物理化学约束的反事实困难负反应生成器**

整体模块：

1. `E`: 反应表征编码器。
2. `A`: 少量负样本校准器。
3. `G`: 反事实编辑生成器。
4. `V`: 物理化学验证器与可行性奖励模型。
5. `C`: semi-hard 课程控制器。
6. `D`: 下游反应预测/逆合成模型，用于评估负样本效用。

### 5.1 模型贡献边界

为了避免与已有 synthetic negative、PU learning、reaction scorer 工作混淆，本文模型贡献应明确落在以下四点：

1. **反应图编辑而非字符串扰动**：模型输出的是反应中心附近的结构编辑动作，再序列化为产物或候选路线。
2. **失败潜空间而非单一负标签**：少量真实负样本用于学习不同失败类型的 latent prototypes，例如区域选择性、条件不兼容、过反应和副产物。
3. **semi-hard 控制而非盲目最难挖掘**：困难度被显式建模，避免 top-k hardest 引入假阴性和训练不稳定。
4. **逆合成排序外部验证**：生成样本必须在独立逆合成候选排序任务中提升表现，不能只在生成器自己的判别器上自证。

### 5.2 最小原型到大模型的实现分层

项目实现分三层推进：

1. **MVP 层**：规则驱动的反应中心反事实编辑器。目标是当天跑通完整流程。
2. **学习层**：用生成样本和少量真实负样本训练轻量 reranker/feasibility model，形成下游收益证据。
3. **大模型层**：训练 graph edit decoder + Transformer/GNN encoder + preference optimization，使用全量数据规模化训练。

## 6. 模块设计

### 6.1 数据表示

每条反应表示为：

```text
x = (R, C, P, y, meta)
```

其中：

- `R`: reactants
- `C`: condition，包括 reagent、solvent、catalyst、temperature、time 等
- `P`: product 或 observed outcome
- `y`: label，正样本、真实负样本、合成负样本、未知样本
- `meta`: 数据来源、反应族、模板、产率、置信度、atom mapping 状态

同时保留三种表示：

1. canonical reaction SMILES
2. randomized reaction SMILES
3. atom-mapped reaction graph

顶刊版本不建议只使用 SMILES，因为审稿人会质疑模型是否真的理解反应中心。必须引入 atom mapping 和 reaction graph edit。

### 6.2 反应表征编码器 E

编码器采用双通道：

```text
h = E_seq(reaction_smiles) || E_graph(G_R, G_P, Delta_bond, C)
```

序列通道学习化学语言，图通道学习反应中心和键变化。图通道至少包含：

- atom type
- formal charge
- valence
- aromaticity
- hybridization
- ring membership
- bond order
- changed bond flag
- leaving group flag
- reaction center mask

预训练任务：

```text
L_pretrain = L_masked_token
           + L_reaction_center
           + L_graph_edit_reconstruction
           + L_condition_masking
           + L_product_prediction
```

### 6.3 少量负样本校准器 A

真实负样本很少，不应直接训练大模型。更合理的方式是学习失败原型：

```text
z_fail_k = Prototype({h_neg_i | type_i = k})
```

失败类型包括：

1. `regio_error`: 区域选择性错误
2. `stereo_error`: 立体选择性错误
3. `chemoselectivity_error`: 化学选择性错误
4. `condition_mismatch`: 条件不兼容
5. `no_reaction`: 底物不反应或低转化
6. `over_reaction`: 过反应
7. `under_reaction`: 欠反应
8. `side_product`: 合理副产物
9. `leaving_group_failure`: 离去基或活化失败
10. `catalyst_ligand_failure`: 催化剂/配体组合失败

如果真实负样本没有失败类型标签，可以先用规则、模板差异、产率、产物结构差异进行弱标注，再人工审查一小部分高价值样本。

### 6.4 反事实编辑生成器 G

生成器不从零生成反应，而是执行：

```text
G(R, C, P+, z_fail, epsilon) -> (P-, C-, t, confidence)
```

推荐两阶段生成。

第一阶段：失败规划器。

```text
t ~ p(t | h)
m ~ p(m | h, t)
```

其中 `t` 是失败类型，`m` 是反应中心扰动区域。

第二阶段：图编辑解码器。

```text
Delta- ~ p(Delta | h, t, m, z_fail)
P- = ApplyEdit(R, Delta-)
```

图编辑动作包括：

- 改变反应中心位置。
- 改变新成键原子。
- 改变离去基。
- 改变氧化态或还原程度。
- 改变亲核/亲电攻击位点。
- 移除关键催化或活化步骤。
- 生成竞争副产物。
- 替换条件使原本反应路径不可行。

### 6.5 物理化学验证器 V

验证器是项目成败关键。它不是单个分类器，而是分层漏斗。

第一层：硬约束。

- RDKit 可解析。
- 价态合法。
- 电荷合理。
- 反应前后原子守恒，若不守恒必须显式记录离去基或副产物。
- 反应中心变化在允许的 edit vocabulary 内。
- 产物与反应物结构相关，不能是随机无关分子。
- 条件字段合法，不能生成不存在的催化剂或溶剂标签。

第二层：软评分。

```text
S_valid      = chemical_validity
S_balance    = atom_and_charge_balance
S_local      = reaction_center_locality
S_close      = similarity_to_positive_reaction
S_fail       = predicted_infeasibility_or_low_yield
S_hard       = downstream_model_confusion
S_diverse    = diversity_against_existing_negatives
S_uncertain  = ensemble_uncertainty
S_fn_risk    = false_negative_risk
```

综合奖励：

```text
Reward = w1*S_valid
       + w2*S_balance
       + w3*S_local
       + w4*S_close
       + w5*S_fail
       + w6*S_hard
       + w7*S_diverse
       - w8*S_fn_risk
```

注意：`S_hard` 不应越大越好，而应落在 semi-hard 区间。若一个候选被多个强模型一致认为高度可行，应降低权重或进入人工审查，不应直接标为负样本。

### 6.6 Semi-hard 课程控制器 C

每轮只选取中等困难样本：

```text
round 1: feasibility score in [0.10, 0.35]
round 2: feasibility score in [0.25, 0.55]
round 3: feasibility score in [0.40, 0.70]
round 4: feasibility score in [0.50, 0.80] with strict false-negative filtering
```

这样避免两个极端：

- 太简单：模型一眼识别，无训练价值。
- 太难：可能是真实可行反应或导致训练不稳定。

## 7. 训练策略

### 7.1 阶段 A：正样本大规模预训练

数据：

- USPTO 正反应。
- ORD 结构化反应。
- 可选：Reaxys/Pistachio 等商业数据，仅在有合法授权时使用。

目标：

- 学习反应语法。
- 学习反应中心。
- 学习条件与产物的关系。

输出：

- 基础反应编码器。
- 基础 forward predictor。
- 基础 graph edit decoder。

### 7.2 阶段 B：真实负样本种子集构建

真实负样本来源：

- HiTEA 等 HTE 数据。
- RegioSQM20 等受控负样本/区域选择性数据。
- ORD 中带低产率或 failed 标记的数据。
- 文献或 ELN 中可合法使用、可结构化的失败反应。

严格标注：

- `real_negative`: 真实实验失败或低产率。
- `computational_negative`: 计算或规则推断负样本。
- `synthetic_negative`: 模型生成合成负样本。
- `unknown`: 未知，不可当负样本。

禁止把未知样本直接当负样本。

### 7.3 阶段 C：少量负样本校准

训练：

```text
L_rank = -log sigmoid(s(R,C,P+) - s(R,C,P-))
```

并加入 supervised contrastive loss：

```text
L_con = contrast(anchor_positive, positive_variant, negative_counterfactual)
```

目标：

- 正样本和真实负样本在表征空间形成合理边界。
- 不要求模型过拟合每个负样本。
- 只学习失败方向和边界形状。

### 7.4 阶段 D：反事实负样本生成

对每个成功反应生成多个候选：

```text
for each positive reaction x:
    infer reaction center
    sample failure type t
    apply constrained graph edits
    validate with V
    retain semi-hard candidates
```

每个候选必须记录：

- 来源正样本 ID。
- 失败类型。
- 图编辑动作。
- 验证器得分。
- false-negative risk。
- 是否通过人工/计算复核。

### 7.5 阶段 E：偏好优化或 RL 精炼

优先推荐 DPO/IPO 类偏好优化，而不是一开始使用 PPO。原因是 DPO 更稳定、可复现性更好。

偏好对：

- chosen: 高质量 semi-hard 负样本。
- rejected: 无效样本、太简单样本、假阴性风险高样本。

训练目标：

```text
L_dpo = -log sigmoid(beta * [
    log pi_theta(y_chosen | x) - log pi_ref(y_chosen | x)
  - log pi_theta(y_rejected | x) + log pi_ref(y_rejected | x)
])
```

### 7.6 阶段 F：下游模型训练与验证

用生成负样本训练：

- reaction feasibility classifier
- forward reaction predictor
- retrosynthesis reranker
- reward model for RL reaction prediction

核心问题不是生成器自己分数高，而是下游模型在真实测试集上是否提升。

## 8. 数据路线

### 8.1 第一阶段公开数据

优先使用公开、可复现数据：

- USPTO：成功反应预训练。
- ORD：结构化反应与条件信息。
- RegioSQM20：区域选择性正负结果。
- HiTEA：真实 HTE 反应筛选数据。

### 8.2 第二阶段可选授权数据

如具备合法授权，可加入：

- Reaxys
- Pistachio
- 内部 ELN
- 公司 HTE 数据

必须记录授权和不可公开数据的处理方式。若论文不能公开原始数据，至少公开处理脚本、统计信息、切分 ID 和可复现实验协议。

### 8.3 数据清洗标准

每条反应需要：

- canonicalization
- deduplication
- atom mapping
- reaction role assignment
- yield normalization
- condition normalization
- reaction class annotation
- split leakage check

切分方式至少三种：

1. random split
2. reaction class split
3. scaffold/template OOD split

顶刊论文必须报告 OOD split，不然说服力不足。

## 9. 实验设计

### E0：数据审计

目标：证明数据来源、切分、标签都可信。

输出：

- 数据规模表。
- 正负样本比例。
- 反应族分布。
- 产率分布。
- 去重率。
- atom mapping 成功率。
- 泄漏检查结果。

### E1：生成负样本内在质量

指标：

- SMILES validity
- graph validity
- atom balance
- charge balance
- reaction center locality
- uniqueness
- novelty
- diversity
- failure type controllability

对比：

- random reactant/product mismatch
- rule-based negatives
- template perturbation
- vanilla VAE
- vanilla Transformer generator
- PC-CNG

### E2：困难度评估

指标：

- 被强 forward model 误判为正的比例。
- downstream model feasibility score 分布。
- semi-hard 占比。
- 与真实正样本的 embedding 距离。
- 与真实负样本的 embedding 距离。

注意：困难度高不代表质量高，必须和 false-negative risk 一起报告。

### E3：假阴性风险评估

必须做：

- 多模型 ensemble 检查。
- 数据库检索，排除已知可行反应。
- 高置信候选人工审查。
- 对少量关键候选可做 xTB/DFT 或 HTE 验证。

报告：

- 丢弃了多少假阴性高风险样本。
- 人工审查一致率。
- 计算验证支持率。
- 若有实验，报告全部结果，包括失败结果，不选择性报告。

### E4：低数据反应预测

核心实验：

在 20/50/100/500 positive shots 下比较：

- positive-only fine-tuning
- random negative augmentation
- rule negative augmentation
- hard negative mining
- PC-CNG synthetic negatives
- real negatives upper bound

指标：

- Top-1 product accuracy
- Top-k accuracy
- validity
- calibration error
- positive accuracy
- negative rejection AUC
- AUPRC

### E5：逆合成排序

任务：

给定目标产物和多个候选前体路线，模型需要把可行路线排在前面，把不可行或低产率路线排在后面。

指标：

- top-k route recall
- MRR
- NDCG
- false-positive route rate
- expert preference agreement

### E6：OOD 泛化

切分：

- reaction family held-out
- scaffold held-out
- template held-out
- condition held-out

目标：证明模型不是记忆模板，而是在学习边界。

### E7：消融实验

必做消融：

- 去掉图编辑，只用 SMILES。
- 去掉物理约束。
- 去掉少量真实负样本校准。
- 去掉 semi-hard curriculum。
- 去掉 false-negative filter。
- 把 DPO 换成普通监督学习。
- 只用规则负样本。
- 只用随机负样本。

## 10. 顶刊级证据链

要冲击顶刊，建议形成如下证据链：

1. 文献与数据审计证明负样本稀缺是真问题。
2. 公开 HTE/RegioSQM 数据证明真实负样本确实提升模型。
3. PC-CNG 生成的负样本比随机/规则负样本更合法、更接近、更困难。
4. PC-CNG 在低正样本设定显著提升下游任务。
5. PC-CNG 在 OOD 反应族仍有效。
6. false-negative 控制机制能识别并剔除高风险候选。
7. 专家审查或小规模实验验证支持一部分生成负样本的失败判断。
8. 代码、数据切分、模型、生成样本和失败案例全部公开或尽可能公开。

## 11. 从0到1落地路线

### 阶段 0：立项与边界定义，1到2周

目标：

- 明确论文问题定义。
- 明确哪些样本是真实负样本，哪些是合成负样本。
- 建立数据记录 schema。

交付物：

- project charter
- data schema
- label ontology
- reproducibility checklist

Go/No-Go：

- 如果不能获得任何真实负样本或 HTE 负样本，项目仍可做方法论文，但顶刊概率下降。

### 阶段 1：数据底座，3到6周

任务：

- 下载并清洗 USPTO/ORD。
- 整理 RegioSQM20/HiTEA。
- 实现 reaction canonicalization。
- 实现 atom mapping pipeline。
- 实现 leakage-aware split。

交付物：

- `data/raw`
- `data/processed`
- `splits/random`
- `splits/ood`
- 数据审计报告

成功标准：

- atom mapping 成功率可接受。
- 训练/验证/测试无重复反应泄漏。
- 真实正负样本统计清晰。

### 阶段 2：基线复现，4到8周

任务：

- 复现 positive-only forward predictor。
- 复现真实负样本对低数据反应预测的提升。
- 实现随机负样本和规则负样本 baseline。

交付物：

- baseline 模型。
- baseline 实验表。
- 复现实验脚本。

成功标准：

- 在低数据设定观察到真实负样本收益。
- 随机负样本效果有限，证明问题存在。

### 阶段 3：PC-CNG MVP，6到10周

先不要直接做复杂大模型。MVP 用反应中心规则/模板编辑器实现：

- 反应中心识别。
- 失败类型采样。
- 局部图编辑。
- RDKit 验证。
- semi-hard 筛选。

交付物：

- constrained reaction edit generator。
- validator。
- 10万到100万级合成负样本候选。
- 质量评估报告。

成功标准：

- 合法率显著高于无约束生成。
- semi-hard 占比显著高于随机负样本。
- 下游低数据预测有初步提升。

### 阶段 4：神经生成器，8到12周

任务：

- 训练 graph edit decoder。
- 加入失败类型 prototype。
- 加入少量真实负样本 adapter。
- 加入 DPO/偏好优化。

交付物：

- PC-CNG neural generator。
- 对比 rule MVP 的提升。
- 可控失败类型实验。

成功标准：

- 比规则 MVP 有更高多样性。
- 保持高合法率。
- 下游收益进一步提升。

### 阶段 5：大规模实验与消融，8到12周

任务：

- 全部 baseline 对比。
- 全部消融。
- 三种数据切分。
- 多个 positive-shot 设置。
- 多个负样本倍率。

交付物：

- 主结果表。
- 消融表。
- OOD 表。
- 失败案例分析。

成功标准：

- 结果稳定，不依赖单一数据集。
- 至少在两个真实/半真实 benchmark 上胜出。

### 阶段 6：专家审查与可选实验验证，4到12周

任务：

- 抽样 200 到 500 个生成负样本做专家审查。
- 对高价值、高争议样本做计算验证。
- 如果资源允许，做小规模 HTE 验证。

交付物：

- expert review protocol。
- inter-annotator agreement。
- 全量审查结果。
- 如有实验，公开全部实验记录。

成功标准：

- 专家认为大多数样本是合理负样本或低可行候选。
- 发现的可行候选不删除不隐瞒，应作为 false-negative analysis 或潜在新反应案例报告。

### 阶段 7：论文与开源，4到8周

任务：

- 写论文。
- 开源代码。
- 开源可公开数据和切分。
- 开源 synthetic negative dataset，并明确标注 synthetic。
- 写 model card 和 data card。

交付物：

- manuscript
- supplement
- code repository
- dataset card
- reproducibility package

## 12. 论文结构建议

### Title 候选

1. Physicochemically Constrained Counterfactual Generation of Negative Chemical Reactions
2. Learning Reaction Feasibility Boundaries from Scarce Negative Data
3. Synthetic Counterfactual Negative Reactions Improve Low-Data Reaction Prediction

### Abstract 逻辑

1. 负反应数据重要但稀缺。
2. 随机负样本过于简单且有假阴性风险。
3. 提出 PC-CNG。
4. 通过少量真实负样本校准和物理化学约束生成 semi-hard negatives。
5. 在低数据、OOD、逆合成排序中提升模型。
6. 通过专家/计算/实验验证控制假阴性风险。

### Main Figures

Figure 1：问题动机与负样本稀缺。

Figure 2：PC-CNG 架构。

Figure 3：生成负样本质量与困难度。

Figure 4：低数据反应预测主结果。

Figure 5：OOD 泛化与逆合成排序。

Figure 6：false-negative 控制和专家/实验验证。

## 13. 风险与应对

### 风险 1：生成样本不是真负样本

应对：

- 明确标注 synthetic counterfactual。
- 引入数据库检索、ensemble、专家审查。
- 不把高风险样本用于强监督，只降权或作为 unknown。

### 风险 2：模型只学会识别生成器伪影

应对：

- 多生成器来源。
- 与真实 HTE 负样本测试集评估。
- 用外部 forward model 和外部数据集测试。

### 风险 3：物理约束过强，生成样本不够困难

应对：

- 课程学习逐步放宽约束。
- 增加条件反事实编辑。
- 用 semi-hard 分数动态调节。

### 风险 4：顶刊创新性不足

应对：

- 不只做数据增强。
- 强调反应可行性边界学习。
- 加入 OOD、假阴性控制、专家/实验验证。
- 公开 benchmark 和 evaluation protocol。

### 风险 5：真实负样本拿不到

应对：

- 先用公开 HTE 和 RegioSQM。
- 与实验组合作获取小规模 HTE。
- 方法论文可继续，但目标期刊需下调。

## 14. 期刊定位

### 顶刊级目标

如果有真实 HTE/实验验证：

- Nature Machine Intelligence
- Nature Chemistry
- Nature Communications
- Science Advances
- JACS
- Angewandte Chemie

### 强计算化学/机器学习期刊

如果主要是公开数据和计算验证：

- Chemical Science
- Digital Discovery
- Journal of Chemical Information and Modeling
- Patterns
- Machine Learning: Science and Technology

客观判断：

- 纯计算、无真实实验验证：顶刊概率中等偏低，但可以冲高水平计算化学期刊。
- 公开 HTE + 专家审查 + 严格 OOD：可冲 Science Advances/Nature Communications。
- 真实新 HTE 验证 + 发现 false-negative 中潜在新反应：具备冲 Nature Chemistry/JACS/Angew 的可能。

## 15. 最小可发表版本

如果资源有限，建议最小可发表版本是：

1. 使用 USPTO 预训练。
2. 使用 RegioSQM20 和 HiTEA 做真实负样本验证。
3. 实现 constrained reaction-center edit generator。
4. 实现 false-negative filter。
5. 证明在 20/50/100 positive-shot 下优于 random/rule/HNM。
6. 做 OOD split。
7. 开源 synthetic negative benchmark。

这个版本不一定够顶刊，但可以形成坚实论文。

## 16. 顶刊版本必须增加的内容

若目标明确是顶刊，应增加：

1. 与实验组合作，获取或产生真实 HTE 负样本。
2. 对生成负样本做专家双盲审查。
3. 对高价值候选做小规模实验。
4. 报告所有实验，包括模型错判和生成失败。
5. 形成可复用 benchmark，而不是只报告一个模型。
6. 证明方法能迁移到多个反应族。

## 17. 项目成功判据

### 技术成功

- 合成负样本合法率 > 90%。
- semi-hard 占比显著高于随机/规则负样本。
- 下游低数据任务稳定提升。
- OOD 不显著退化。

### 科研成功

- 证明少量负样本可以校准大规模合成负样本生成。
- 证明反应中心反事实编辑比无约束生成更可靠。
- 建立假阴性控制协议。
- 形成可复用 benchmark 和开源数据。

### 顶刊成功

- 真实 HTE 或实验验证支持。
- 有明确科学 insight，而不只是模型性能提升。
- 生成负样本揭示了反应可行性边界或失败机制。

## 18. 最后判断

这个项目具备可行性和创新性，但冲击顶刊需要非常严格的证据链。真正有价值的不是“生成很多负样本”，而是证明：

1. 这些负样本不是随机错误。
2. 它们靠近真实反应可行性边界。
3. 它们能提升真实低数据任务。
4. 它们的假阴性风险被严肃控制。
5. 生成过程能提供化学可解释性。

如果按这个路线推进，该项目可以从一个 idea 成长为一个严谨的、可发表的化学 AI 研究项目。若进一步加入真实 HTE 或实验验证，它具备冲击顶刊的潜力。

## 19. 当前实现状态

本轮已开始执行项目实现，当前状态如下。

### 19.1 已完成的最小原型

新增模块：

```text
code/chem_negative_sampling/pc_cng/
```

已实现：

1. `chem_utils.py`：轻量 SMILES/reaction 解析、原子 token、近似原子计数、相似度、可选 RDKit 增强。
2. `validator.py`：物理化学启发式验证器，输出 validity、atom balance、locality、hard score、false-negative risk。
3. `counterfactual.py`：反事实负样本生成器，支持 forward outcome negatives 和 retrosynthesis precursor negatives。
4. `reranker.py`：零重依赖轻量 logistic reranker，用于验证生成样本是否能形成逆合成候选排序训练信号。
5. `run_mvp.py`：端到端 MVP 命令，自动生成 demo 数据、合成负样本、训练轻量 reranker、输出 metrics。
6. `run_scale_generation.py`：面向全量数据的 streaming 生成入口，避免一次性读入全量 USPTO/ORD。
7. `README.md`：当前原型运行说明。

### 19.2 已跑通的命令

在 `code/chem_negative_sampling` 下执行：

```bash
python3 -m pc_cng.run_mvp --output-dir results/pc_cng_mvp_demo --epochs 200
python3 -m pc_cng.run_scale_generation \
  --input results/pc_cng_mvp_demo/positive_reactions.csv \
  --output results/pc_cng_scale_smoke/synthetic_negatives.csv \
  --summary results/pc_cng_scale_smoke/summary.json \
  --limit 5
```

### 19.3 当前 MVP 结果

Demo MVP：

- 正样本：10 条。
- 合成反事实负样本：60 条。
- forward outcome negatives：30 条。
- retrosynthesis precursor negatives：30 条。
- 平均 hard score：约 0.786。
- 平均 false-negative risk：约 0.480。
- 轻量逆合成排序 smoke-test：top1 约 0.667，MRR 约 0.833，NDCG 约 0.877。

这些结果只能说明代码闭环已跑通，不能作为论文性能声明。

### 19.4 下一步工程重点

1. 安装/配置 RDKit、PyTorch、PyTorch Geometric、RXNMapper。
2. 接入真实公开数据：RegioSQM20、HiTEA、USPTO/ORD。
3. 实现 atom-mapped reaction center extraction。
4. 把当前规则编辑器替换为可学习 graph edit decoder。
5. 建立真实 baseline：random negative、rule negative、template perturbation、DORA-style alternate center、PU negative。
6. 在逆合成候选排序任务上做严格 train/test/OOD split。
7. 使用全量数据训练大模型并进行消融、OOD、false-negative 审查。


---

## 20. 当前实现真实状态审计（2026-07-19）

本节是对 Section 19 的实质性更新。Section 19 描述的是 MVP 阶段，但项目实际已经推进到 v3 阶段，并产出 manuscript 级表格。需要把"文档主张"与"代码与结果实际状态"对齐，避免后续执行者按 Section 19 的过时信息决策。

### 20.1 已落地能力（代码 + 结果 + 测试三方对齐）

| 能力 | 代码位置 | 关键结果 | 测试 / 审计 |
|---|---|---|---|
| 规则反应中心边界负样本生成 | `chem_negative_sampling/pc_cng/reaction_boundary_generator.py`、`atom_mapped_graph_edit.py` | 已生成 reviewed synthetic negatives，覆盖 RegioSQM20 + HITEA + USPTO/OpenMolecules expanded M3 | `tests/test_graph_stats_featurizer.py`、benchmark data-quality audit `pass_with_warnings` |
| 同上下文候选重排序（Type-1 主任务） | `train_pairwise_reward_mlp.py` | Overall Top-1 97.49 ± 0.06%；Held-out Test Top-1 85.07 ± 0.94%（10 seeds） | `multiseed_paired_significance.py`；paired CI / permutation p / sign-test p 三层检验 |
| Combined Morgan + GraphStats 特征 | `train_pairwise_reward_mlp.py --feature-mode combined` | Expanded curated scope Top-1 84.66% → 89.82%（+5.16 pp, p < 0.0001, 10/10 seeds） | `tests/test_paired_reranking_significance.py` |
| 外部 product-selection bridge（validity-aware） | `evaluate_external_product_prediction_benchmark.py` | PC-CNG Morgan 5-seed Test Top-1 98.50 vs Chemformer likelihood 0.07（15,973 groups / 174,908 rows） | `tests/test_external_product_prediction_benchmark.py` |
| 外部严格 shared intersection | 同上 | PC-CNG 71.60 vs Chemformer 4.94（1,197 groups, apples-to-apples 学习型打分器对比） | 同上 |
| 外部 repaired 25k strict 全候选打分 | `train_external_score_calibrator.py`、`train_external_score_mlp_calibrator.py` | Chemformer 57.00% > PC-CNG 13.59% > best hybrid 50.87%（PC-CNG 作为主打分器为负结果） | `external_score_calibration_25k_repaired_strict_20260715/` |
| Reaction-class gate（Optimization C） | `build_curated_weak_class_contexts.py`、`type1_unreacted_v2_multiseed` | Hydrogenation / Rh / Amide-Cu 弱类修复，classw050_rc 在 expanded curated scope 达 97.16 ± 0.30% | `tests/test_reaction_class_benchmark.py`、`tests/test_curated_weak_class_contexts.py` |
| Science Advances 风格 split + 10-seed benchmark | `build_science_advances_regiosqm_splits.py`、`summarize_science_advances_benchmark.py` | K_low ΔTop-1 +2.83 ± 1.78 pp（CI [1.72, 3.94]）；K_high ΔTop-1 +1.91 ± 1.20 pp（CI [1.20, 2.67]） | `tests/test_multiseed_paired_significance.py` |
| Type-2 low-yield 二分类 | `train_feasibility_mlp.py` | Test ROC-AUC 85.56 ± 0.21%；AUPRC 79.93 ± 0.08%；F1 72.30 ± 0.08% | 10-seed multiseed summary |
| Manuscript 表格体系（10 张主/补表） | `build_manuscript_tables.py` | `results/manuscript_tables_pc_cng_v3/manifest.json` 含 10 csv+md 对 | manifest SHA256 记录于 reproducibility manifest |
| Reproducibility manifest | `PC-CNG-v3-reproducibility-manifest-20260712.{md,json}` | 固定输入、queue 顺序、result 目录、SHA-256 hash 全部记录 | benchmark data-quality audit + remote SHA-256 校验 |
| Held-out 5k external calibration protocol | `PC-CNG-v3-external-heldout-calibration-protocol-20260715.{md,json}` | 5,000 USPTO/OpenMolecules test contexts，base-only MLP Top-1 94.51%（positive diagnostic，非 full-beam SOTA evidence） | base quality audit `pass`；full-beam 评分 pending |

### 20.2 仍停留在文档/原型阶段、尚未落地的能力

文档 Section 5–7 主张的 PC-CNG 完整架构，目前**只有约 30% 落地**。下表标注差距，避免后续论文写作过度主张。

| 文档主张的模块 | 当前实际状态 | 差距类型 |
|---|---|---|
| 双通道反应表征编码器 E（sequence + graph edit） | 仅 Morgan fingerprint + GraphStats 统计向量；无 Transformer 序列通道；无 atom-mapped graph attention | 架构未实现 |
| 少量负样本校准器 A（失败原型 prototype learning） | 未实现 prototype learning；真实负样本只用于 pairwise margin 与 DPO reference | 模块未实现 |
| 反事实编辑生成器 G（failure planner + graph edit decoder） | `reaction_center_edit_decoder.py` 仅为规则编辑 + 启发式 anchor 排序；`train_graph_edit_decoder.py` 只是浅层 MLP 分类器，不是 GNN decoder | 生成器仍是规则 |
| 物理化学验证器 V（分层漏斗 + 综合奖励） | `validator.py` 输出 hard score、false-negative risk，但未集成进训练 reward；无 ensemble 不确定性 | 验证器未串联 |
| Semi-hard 课程控制器 C（多轮 feasibility 区间） | 未实现；当前是一次性生成 + 过滤 | 课程未实现 |
| DPO/IPO 偏好优化 | 已实现但 best DPO branch（beta050 overall 91.16%）仍逊于 pairwise_default 97.49% | 已实现但未达主线 |
| 大模型层（graph edit decoder + Transformer/GNN + 全量数据） | 未训练；USPTO 仅作为 candidate pool，未作为预训练源 | 大模型未启动 |
| False-negative 三层控制（多模型 ensemble + 数据库检索 + 专家审查） | 仅 ensemble 一层（5-seed Morgan）；无数据库检索 pipeline；无专家审查 | 控制不完整 |
| 真实 HTE 实验验证 | 无 | 完全缺失 |
| 真实失败反应数据扩展（ORD、Reaxys、Pistachio、ELN） | 仅 RegioSQM20 + HITEA + USPTO/OpenMolecules；ORD 未接入；Reaxys/Pistachio 未授权 | 数据源单一 |

### 20.3 主结论可主张边界（论文级口径）

基于 Section 20.1–20.2，论文可主张的最大边界：

```text
PC-CNG v3 provides a reproducible, statistically rigorous (10-seed paired
significance) boundary-negative generation and same-context candidate
reranking framework. Under a pre-declared external candidate-ranking
protocol, the learned PC-CNG scorer substantially outperforms frozen
Chemformer conditional likelihood on the validity-aware candidate set, and
matches or exceeds Chemformer on the strict shared-intersection subset.
Combined Morgan + graph-stats features yield a statistically significant
+5.16 pp improvement on the expanded curated weak-class scope. The framework
also reproduces the Science Advances low-positive-data boost on
RegioSQM20-derived K_low/K_high splits.
```

论文**不能**主张：

```text
- End-to-end forward product generation SOTA（与 Molecular Transformer >90% 不可同表）
- Strict external 25k 上 PC-CNG scorer 优于 Chemformer（实际 13.59% < 57.00%）
- 超越 RegioSQM20 site-selectivity SOTA（held-out test 85.07% < 92.7%，差 7.63 pp）
- 真实 HTE 或实验验证支持
- 假阴性风险已被三层控制（实际只有 ensemble 一层）
- 反应中心图编辑解码器（G 模块）已可学习（实际仍是规则）
```

---

## 21. 文档九维自审

按用户问题逐一审计。每条给出"判断 + 证据 + 改进建议"。

### 21.1 目标清晰度

**判断：中等偏清晰，但缺验收门槛。**

- 优点：Section 1–4 把"为什么做、解决什么、主张什么"凝练得很好；Section 3 的论文主张段落可直接作为 abstract 雏形。
- 不足：
  1. 没有"什么算完成"的量化判据。Section 17 的"技术成功"过于宽泛（"显著高于"、"稳定提升"无阈值）。
  2. 没有"什么算放弃"的退出条件。例如：若 held-out 5k full-beam 仍逊于 Chemformer，是否放弃 external bridge 主线？
  3. 没有阶段性 Go/No-Go 量化指标。Section 11 的阶段 0–7 只给"交付物"和"成功标准"，但成功标准未与已落地结果挂钩。

**改进建议**：在 Section 22 的下一阶段目标中加入量化 Go/No-Go 表（每个任务给出 primary metric + 阈值 + 退出条件）。

### 21.2 TODO 细致度

**判断：偏粗，不适合直接交给 trae 执行。**

- 优点：Section 11 给出 8 阶段 + 交付物，方向正确。
- 不足：
  1. Section 19.4 的"下一步工程重点"停留在 v1 MVP 阶段（"安装 RDKit、接入公开数据"），与 v3 已完成状态脱节。
  2. 没有任务编号、责任人占位、依赖关系、预计工时、阻塞条件。
  3. 没有区分"科研任务"和"工程任务"。trae 难以判断哪些任务可由 sub-agent 自动执行、哪些需要人工审查。
  4. 现有 100+ 个 `scripts_run_*.sh` 与 60+ 个 `pc_cng/*.py` 已构成事实工程基线，但文档未把它们组织成可执行的下一步清单。

**改进建议**：Section 22 给出 P1-00 至 P1-13 任务清单，每项含目标、输入、输出、验收命令、降级路径。

### 21.3 SOTA 可行性

**判断：候选重排序可达 SOTA-positive 主张；端到端生成不可达。**

- 已达 SOTA-positive 的子任务：
  1. Same-context candidate reranking（Overall 97.49%）— 但同上下文候选构造本身偏向 PC-CNG，不算严格 SOTA。
  2. Validity-aware external bridge（PC-CNG 98.50 vs Chemformer 0.07）— 不 apples-to-apples，需在论文中明确"validity-aware"口径。
  3. Strict shared intersection external bridge（PC-CNG 71.60 vs Chemformer 4.94）— 当前最强 apples-to-apples 证据。
  4. Combined 特征 expanded curated scope +5.16 pp（p < 0.0001）— 架构消融主证据。
  5. Science Advances K_low/K_high ΔTop-1 CI 全正 — 复现并扩展了 SciAdv 核心结论。

- 不可达 SOTA 的子任务：
  1. End-to-end forward product top-1（与 Molecular Transformer >90% 不同任务定义）。
  2. RegioSQM20 site-selectivity（held-out test 85.07% < 92.7%）。
  3. External repaired 25k strict 全候选打分（PC-CNG 13.59% < Chemformer 57.00%）— PC-CNG 作为外部独立打分器仍是负结果。
  4. Ni coupling 反应族（数据源缺口）。

**改进建议**：Section 22 P1-01 必须闭环 held-out 5k full-beam 评估；若仍负，文档需明确把 external bridge 降级为"validity-aware supplement"而非 main claim。

### 21.4 顶刊潜力

**判断：当前证据链可冲 SciAdv / Nat Commun 级别（中等概率），冲 Nature Chemistry / JACS / Angew 需真实 HTE（当前 0 概率）。**

- 已具备的顶刊证据要素：
  1. 严格统计：10-seed paired significance + permutation p + sign-test p（顶刊标配）。
  2. 复现并扩展 SciAdv 2025 论文核心实验（K_low/K_high）。
  3. 多个 ablation（DPO beta sweep、combined 特征、hidden_dim、dropout、nbits、coslr、pairwise/margin）。
  4. 严格 benchmark manifest + reproducibility manifest + SHA-256 hash。
  5. 明确负结果披露（external repaired 25k strict PC-CNG 13.59% < Chemformer 57.00%）。

- 仍缺的顶刊证据要素：
  1. 真实 HTE 实验验证（无）。
  2. 专家双盲审查 synthetic negatives（无）。
  3. 真实失败反应数据扩展（无 ORD / Reaxys / Pistachio）。
  4. 三层 false-negative 控制（仅 ensemble 一层）。
  5. 多反应族迁移证据（仅 RegioSQM20 + HITEA + USPTO，Ni 缺失）。
  6. 反应中心图编辑解码器（仍是规则）。

**改进建议**：Section 22 P1-08 引入专家审查协议（200–500 条 synthetic negatives 双盲）；P1-09 接入 ORD；P1-10 探索小规模 HTE 合作或计算验证（xTB/DFT）。

### 21.5 科研故事

**判断：故事核心强，但当前实现与故事主张存在断层。**

- 强故事点：
  1. "少量真实负样本校准失败方向" — H1 假设清晰，符合 SciAdv 2025 论文方向。
  2. "反应中心编辑 vs 从零生成" — H2 假设符合化学直觉。
  3. "Semi-hard 课程 vs top-k hardest" — H3 假设有训练动力学价值。
  4. "False-negative 风险显式控制" — 直击审稿人核心质疑。
  5. "Boundary-negative 改善低数据反应预测" — H4/H5 假设可量化。

- 故事断层：
  1. **主张的是生成模型，实际是规则生成器 + 学习型 reranker**。审稿人会问："你的 G 模块在哪里？图编辑解码器在哪里？" 当前 `train_graph_edit_decoder.py` 是浅 MLP，不能作为"graph edit decoder"在论文里主张。
  2. **主张少量真实负样本校准，实际未实现 prototype learning**。真实负样本只是 pairwise margin 与 DPO reference 的负样本，没有显式的"失败方向"学习。
  3. **主张 semi-hard 课程，实际是一次性过滤**。审稿人会质疑 H3 假设未真正验证。
  4. **主张 false-negative 三层控制，实际只有 ensemble 一层**。

**改进建议**：Section 22 P1-05 必须实现可学习 graph edit decoder（至少一版 GNN），让 G 模块能写进论文；P1-06 实现 prototype learning 校准器；P1-07 实现 semi-hard 课程控制器（即使只跑 1 个 round 对比一次性过滤）。

### 21.6 数据集全面性

**判断：覆盖核心三源，但缺失多个顶刊必备数据源。**

- 已接入：
  1. RegioSQM20（EAS 区域选择性，正负配对）— `data/processed/regiosqm20_normalized.csv`
  2. HITEA（真实 HTE 筛选，含低产率）— `data/processed/hitea_full_normalized.csv`
  3. USPTO/OpenMolecules（成功反应大规模预训练池）— `data/processed/uspto_openmolecules_normalized.csv`，530,238 行

- 缺失（顶刊必备）：
  1. **ORD（Open Reaction Database）** — 结构化反应 + 条件字段，是顶刊标配。当前完全未接入。
  2. **Reaxys / Pistachio** — 商业授权，可作可选。
  3. **ChEMBL reactions** — 生物活性分子反应，扩展反应族覆盖。
  4. **Ni coupling 数据** — HITEA 0 个、USPTO 6 个，是已知硬缺口。
  5. **真实失败反应 ELN / 文献挖掘数据** — 顶刊升级版必备。
  6. **外部 forward predictor 测试集** — 如 USPTO-MIT、USPTO-Lexicon 用于 forward prediction 评测，目前完全缺失。

**改进建议**：Section 22 P1-09 接入 ORD（公开、结构化、有条件字段）；P1-11 调研 Ni coupling 文献挖掘或与实验组合作。

### 21.7 下游任务全面性

**判断：覆盖 reranking，但缺多个顶刊标配下游任务。**

- 已覆盖：
  1. Same-context candidate reranking（Type-1 主任务）。
  2. External product-selection bridge（external candidate reranking）。
  3. Low-yield binary classification（Type-2 辅助任务）。
  4. Science Advances K_low/K_high 复现。
  5. Reaction-class gate（弱类修复）。

- 缺失：
  1. **End-to-end forward reaction prediction**（Molecular Transformer 任务）— 顶刊会要求对比。
  2. **Retrosynthesis route ranking**（多步路线排序，非单步前体排序）— Section 9 E5 提出但未实现。
  3. **Reaction feasibility classification on external test set**（独立测试集）。
  4. **Reward model for RL-based reaction prediction**（SciAdv 2025 论文核心下游）— Section 7.6 提出但未实现。
  5. **Reaction class prediction**（多类分类）— 作为辅助任务。
  6. **Yield regression**（连续产率预测）— 比 Type-2 二分类更细。

**改进建议**：Section 22 P1-12 增加 retrosynthesis route ranking 任务（使用 AiZynthFinder 或 Retro*）；P1-13 增加 reward model for RL 任务。

### 21.8 测评全面性

**判断：统计严谨性强，但真实性、多样性、外部性不足。**

- 已具备：
  1. 10-seed paired significance（group-level + seed-level 双层 bootstrap CI）。
  2. Permutation test + sign-test 双 p 值。
  3. Strict shared intersection + validity-aware 双协议。
  4. Multiple splits（random / reaction class / scaffold-template OOD）— 文档主张，实际仅部分实现。
  5. Ablation 矩阵（DPO beta、hidden_dim、dropout、nbits、coslr、pairwise/margin、combined 特征）。
  6. Reproducibility manifest + SHA-256 hash。
  7. Benchmark data-quality audit + 单元测试。

- 缺失：
  1. **真实 HTE 测试集评估**（无）。
  2. **专家审查协议**（无）。
  3. **跨数据集迁移评估**（无，例如训练 RegioSQM20 测试 HITEA）。
  4. **多 forward model 集成 false-negative 检查**（仅 5-seed Morgan ensemble，未引入 Chemformer / Molecular Transformer 共同检查）。
  5. **数据库检索 false-negative 检查**（未实现，例如对生成 negative 检索 USPTO/ORD/Reaxys 是否已知正反应）。
  6. **Calibration error**（文档 Section 9 E4 提到，实际未报告）。
  7. **OOD scaffold/template/condition held-out 完整执行**（文档主张，实际仅 reaction class held-out）。

**改进建议**：Section 22 P1-08 引入专家审查 + 数据库检索 false-negative 检查；P1-04 增加 cross-dataset 迁移评估；P1-07 增加 calibration error 报告。

### 21.9 模型架构最优性

**判断：当前架构远非最优，是论文最大短板。**

- 当前主架构：Morgan fingerprint (n_bits=4096) + 2-layer MLP (hidden=2048) + pairwise margin loss。这是 2018 年水平的化学 ML 架构。
- 已尝试的架构升级：
  1. hidden_dim 4096 — 不显著（p=0.629）。
  2. dropout 0.4 — 无效。
  3. nbits 8192 — 不显著（p=0.489）。
  4. Cosine LR + warmup — 不显著（p=1.000）。
  5. Pairwise weight / margin sweep — 不显著。
  6. Combined Morgan + GraphStats — 在 expanded curated scope 显著（+5.16 pp, p<0.0001），但在 original scope 不显著。
  7. DPO beta sweep — 不超 pairwise default。

- 未尝试的架构升级（顶刊必备）：
  1. **Atom-mapped graph neural network**（MPNN / Graph Transformer）— 文档 Section 5.2 主张双通道，实际未实现。
  2. **Reaction SMILES Transformer**（如 Molecular Transformer / Chemformer 微调）— 仅用 Chemformer 作为 frozen reference，未作为可学习 backbone。
  3. **Graph edit decoder**（文档 Section 6.4 主张）— `train_graph_edit_decoder.py` 仅浅 MLP。
  4. **Prototype learning for failure modes**（文档 Section 6.3 主张）— 未实现。
  5. **Multi-task learning**（reranking + yield regression + class prediction）— 未实现。
  6. **Pre-training on USPTO with masked edit objective**（文档 Section 6.2 主张）— 未实现。

**改进建议**：Section 22 P1-05 必须实现一版 GNN-based reaction encoder（至少 MPNN + reaction center attention），即使只在小数据上验证可行性；这是论文从"方法学论文"升级到"顶刊"的必要条件。

---

## 22. 下一阶段 P1 目标（trae 执行版）

**目标定位**：把 PC-CNG v3 从"可写成方法学论文初稿"推进到"具备顶刊投稿证据链"。本阶段不追求一次到位实现 Section 5–7 全部架构，而是补齐 5 个最关键的断层：可学习生成器、真实数据扩展、专家审查、跨任务评估、HTE/计算验证。

**预计阶段**：P1（持续 4–6 周，分两批任务）

**资源约束**：
- 服务器：`cunyuliu@36.137.135.49`，GPU 0–7（需与现有 RF-CF5 PID 1437378 / 子进程 2042374、calibrate PID 2544995 共享，禁止占用 GPU 4）
- 数据目录：`/home/cunyuliu/pc_cng_research/`
- 代码目录：`/home/cunyuliu/pc_cng_research/chem_negative_sampling/pc_cng/`
- 历史产物：`results/`、`docs/` 下已有 161 + 38 个目录/文件，禁止删除或重命名已有 result 子目录
- 测试基线：`tests/` 下 17 个测试文件必须保持通过

**优先级排序**（依用户偏好：计算/算法壁垒 > 真实数据 > 实验）：
1. P1-05 可学习 graph edit decoder（算法壁垒，最高优先级）
2. P1-06 失败原型 prototype learning（算法壁垒）
3. P1-07 semi-hard 课程控制器（算法壁垒）
4. P1-09 ORD 接入（数据扩展）
5. P1-08 专家审查协议 + false-negative 三层控制（评测严谨性）
6. P1-01 held-out 5k full-beam 评估闭环（已有外部 bridge 闭环）
7. P1-02 cross-dataset 迁移评估
8. P1-03 calibration error + OOD scaffold/template 完整执行
9. P1-04 retrosynthesis route ranking 任务
10. P1-10 小规模 HTE 或 xTB/DFT 计算验证
11. P1-11 Ni coupling 数据缺口调研
12. P1-12 manuscript v1 草稿（含 figures）
13. P1-13 项目文件结构整理（独立任务，可并行）

### 22.1 任务清单

#### P1-00 阶段启动与基线锁定（必须先做）

**目标**：固化 P1 起点状态，避免后续结果不可追溯。

**输入**：当前 `results/` 与 `docs/` 全量。

**输出**：
- `docs/P1_initial_state_manifest_20260719.json`：记录 P1 启动时所有 result 目录 SHA-256、当前 manuscript 表格清单、当前 best 模型 checkpoint 路径。
- `docs/P1_baseline_metrics_20260719.md`：记录当前 best 主张与负结果，作为 P1 结束时对比基线。

**验收命令**：
```bash
python3 -m pc_cng.audit_active_execution_status --output-dir results/P1_initial_state_20260719
python3 -m pc_cng.build_manuscript_tables --output-dir results/manuscript_tables_p1_baseline_20260719
```

**Go/No-Go**：必做，无门槛。

#### P1-01 Held-out 5k full-beam 评估闭环

**目标**：完成 `external_calibration_heldout_5k` 的 full-beam Chemformer 生成 + 全候选打分 + frozen MLP 应用，得到真正预声明的 held-out external SOTA 证据。

**输入**：
- `results/external_calibration_heldout_contexts_5k_20260715/`（已选 5,000 contexts）
- `results/external_calibration_heldout_pc_cng_targeted_5k_20260715/`（已生成 PC-CNG negatives）
- `results/external_score_mlp_calibrator_v1_uspto12k_apply_heldout_base5k_20260716/`（base-only MLP diagnostic）

**输出**：
- `results/external_calibration_heldout_full_beam_5k_202607XX/benchmark_summary.json`
- 主张文档更新：若 MLP Top-1 ≥ Chemformer baseline + 1.0 pp 且 paired CI 全正 → 升级 external bridge 为 main claim；否则降级为 "validity-aware supplement"。

**验收命令**：
```bash
bash scripts_run_external_product_prediction_25k_chunked_beams.sh \
  --input results/external_calibration_heldout_contexts_5k_20260715/contexts.csv \
  --output results/external_calibration_heldout_full_beam_5k_202607XX
python3 -m pc_cng.evaluate_external_product_prediction_benchmark \
  --candidates results/external_calibration_heldout_full_beam_5k_202607XX/full_candidates.csv \
  --output-dir results/external_calibration_heldout_full_beam_5k_202607XX/benchmark
python3 -m pc_cng.train_external_score_mlp_calibrator \
  --apply-recipe results/external_score_mlp_calibrator_v1_uspto12k_train_20260716 \
  --apply-input results/external_calibration_heldout_full_beam_5k_202607XX/full_candidates.csv \
  --apply-output results/external_calibration_heldout_full_beam_5k_mlp_apply_202607XX
```

**Go/No-Go**：
- 若 frozen MLP held-out Top-1 ≥ Chemformer + 1.0 pp 且 paired CI 全正 → external bridge 升级为 main claim；
- 若 frozen MLP held-out Top-1 < Chemformer → external bridge 降级为 validity-aware supplement，论文 Section 14 期刊定位下调到"强计算化学/机器学习期刊"。

**降级路径**：若 full-beam Chemformer 生成仍因 GPU 资源失败，改为 chunked beam + 5 个独立 seed MLP ensemble，并明确标注"non-full-beam diagnostic"。

#### P1-02 Cross-dataset 迁移评估

**目标**：证明 PC-CNG 学到的 boundary negatives 可跨数据集迁移。

**输入**：RegioSQM20、HITEA、USPTO/OpenMolecules processed 数据。

**输出**：
- `results/cross_dataset_transfer_202607XX/`：包含 4 个迁移对（RegioSQM20→HITEA、HITEA→RegioSQM20、RegioSQM20→USPTO-subset、HITEA→USPTO-subset）的 10-seed 评估。
- 主张：若跨数据集 ΔTop-1 > 0 且 CI 全正 → 加入论文 Section 9 E6 OOD 泛化表。

**验收命令**：
```bash
python3 -m pc_cng.run_cross_dataset_transfer_eval \
  --source regiosqm20 --target hitea \
  --seeds 20260710-20260719 \
  --output-dir results/cross_dataset_transfer_202607XX
```

**Go/No-Go**：CI 全正 → 写入论文；CI 跨 0 → 仅作为 supplementary diagnostic。

#### P1-03 Calibration Error + OOD Scaffold/Template 完整执行

**目标**：补齐 Section 9 E4/E6 缺失指标。

**输入**：当前 v2/unreacted 10-seed 模型。

**输出**：
- `results/calibration_error_10seed_202607XX/`：报告 ECE、MCE、Brier score。
- `results/ood_scaffold_template_split_202607XX/`：scaffold held-out + template held-out 切分评估。

**验收命令**：
```bash
python3 -m pc_cng.evaluate_calibration_error \
  --model-dir results/type1_unreacted_substrate_supplement_v2_20260711 \
  --output-dir results/calibration_error_10seed_202607XX
python3 -m pc_cng.run_ood_scaffold_template_eval \
  --seeds 20260710-20260719 \
  --output-dir results/ood_scaffold_template_split_202607XX
```

**Go/No-Go**：必须报告，无门槛（即使结果负也要写入 supplementary）。

#### P1-04 Retrosynthesis Route Ranking 任务

**目标**：补齐 Section 9 E5 缺失任务。

**输入**：USPTO 多步路线数据（可从 USPTO-MIT-50k 派生）。

**输出**：
- `results/retrosynthesis_route_ranking_202607XX/`：含 top-k route recall、MRR、NDCG、false-positive route rate。
- 工具：AiZynthFinder 或 Retro*（若安装受阻，降级为 USPTO-MIT-50k 单步前体重排）。

**验收命令**：
```bash
python3 -m pc_cng.run_retrosynthesis_route_ranking \
  --routes-data data/processed/uspto_mit_50k_routes.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --output-dir results/retrosynthesis_route_ranking_202607XX
```

**Go/No-Go**：若 PC-CNG augmented reranker 在 MRR 上 > baseline + 1.0 pp 且 CI 全正 → 写入论文 main table；否则降级为 supplementary。

#### P1-05 可学习 Graph Edit Decoder（算法壁垒，最高优先级）

**目标**：把当前规则 `reaction_center_edit_decoder.py` 升级为可学习 GNN decoder，让 Section 6.4 的 G 模块真正落地。

**输入**：
- `chem_negative_sampling/pc_cng/atom_mapped_graph_edit.py`（已有反应中心抽取）
- `chem_negative_sampling/pc_cng/reaction_center_edit_decoder.py`（规则版）
- USPTO/OpenMolecules atom-mapped reactions（用 RXNMapper 补 mapping）

**输出**：
- `chem_negative_sampling/pc_cng/learned_graph_edit_decoder.py`：基于 MPNN + reaction center attention 的可学习 decoder。
- `chem_negative_sampling/pc_cng/train_learned_graph_edit_decoder.py`：训练入口。
- `results/learned_graph_edit_decoder_202607XX/`：与规则版对比的 10-seed 评估。
- 单元测试：`tests/test_learned_graph_edit_decoder.py`。

**验收命令**：
```bash
python3 -m pc_cng.train_learned_graph_edit_decoder \
  --train-data data/processed/uspto_openmolecules_train_only.csv \
  --val-data data/processed/regiosqm20_normalized.csv \
  --output-dir results/learned_graph_edit_decoder_202607XX \
  --seeds 20260710-20260719
python3 -m pytest tests/test_learned_graph_edit_decoder.py -v
```

**Go/No-Go**：
- 若 learned decoder 在生成多样性 + 合法率 + downstream reranking Test Top-1 上均 ≥ 规则版 → 升级为论文 main architecture；
- 若 learned decoder 在 Test Top-1 上 < 规则版但多样性显著更高 → 作为"architecture supplement"写入 supplementary；
- 若 learned decoder 完全失败 → 写入 ablation 表作为"learned decoder v1 negative result"，论文保留规则版作为 main generator，但需明确文档 Section 6.4 主张与实现差距。

**降级路径**：若 MPNN 训练不稳定，降级为 GraphSAGE + edge prediction；再不稳定，降级为 GAT + reaction center mask。

#### P1-06 失败原型 Prototype Learning 校准器

**目标**：实现 Section 6.3 主张的失败原型学习，让真实负样本显式校准失败方向。

**输入**：HITEA 低产率真实负样本 + RegioSQM20 alternative-outcome 真实负样本。

**输出**：
- `chem_negative_sampling/pc_cng/failure_prototype_calibrator.py`：基于 prototype network 的失败方向学习器。
- `results/failure_prototype_calibration_202607XX/`：含失败类型可控性指标（10 种失败类型分类 accuracy）。
- 单元测试：`tests/test_failure_prototype_calibrator.py`。

**验收命令**：
```bash
python3 -m pc_cng.train_failure_prototype_calibrator \
  --real-negatives data/processed/hitea_full_normalized.csv \
  --output-dir results/failure_prototype_calibration_202607XX
python3 -m pytest tests/test_failure_prototype_calibrator.py -v
```

**Go/No-Go**：
- 若 prototype 分类 accuracy ≥ 0.70 且生成的 negatives 在 failure type 上可控（entropy > uniform） → 写入论文 Section 6.3；
- 否则作为 ablation 写入 supplementary。

#### P1-07 Semi-hard 课程控制器

**目标**：实现 Section 6.6 主张的 semi-hard 课程，验证 H3 假设。

**输入**：当前 boundary negatives + feasibility score 分布。

**输出**：
- `chem_negative_sampling/pc_cng/semi_hard_curriculum.py`：4 轮课程控制器。
- `results/semi_hard_curriculum_4round_202607XX/`：与一次性过滤对比的 10-seed 评估。

**验收命令**：
```bash
python3 -m pc_cng.run_semi_hard_curriculum \
  --rounds 4 \
  --intervals "[0.10,0.35]" "[0.25,0.55]" "[0.40,0.70]" "[0.50,0.80]" \
  --output-dir results/semi_hard_curriculum_4round_202607XX
```

**Go/No-Go**：
- 若 curriculum 在 Test Top-1 上 > one-shot + 0.5 pp 且 CI 全正 → 写入论文 main training strategy；
- 否则作为 ablation 写入 supplementary，并在论文中明确"H3 假设未在本数据规模下显著验证"。

#### P1-08 专家审查协议 + False-Negative 三层控制

**目标**：补齐 Section 9 E3 缺失的真实性与三层控制。

**输入**：当前 reviewed synthetic negatives（约 64,646 行）。

**输出**：
- `docs/expert_review_protocol_202607XX.md`：双盲审查协议（含 200–500 条抽样规则、审查表单、inter-annotator agreement 计算方法）。
- `results/expert_review_202607XX/`：审查结果（若实际执行）或审查协议占位（若未找到专家）。
- `chem_negative_sampling/pc_cng/false_negative_three_layer_control.py`：ensemble + 数据库检索 + 专家审查三层融合。
- `results/false_negative_three_layer_202607XX/`：三层控制后的 high-confidence synthetic negatives 子集。

**验收命令**：
```bash
python3 -m pc_cng.run_false_negative_three_layer_control \
  --ensemble-models results/type1_unreacted_substrate_supplement_v2_20260711 \
  --database data/processed/uspto_openmolecules_normalized.csv \
  --expert-review results/expert_review_202607XX \
  --output-dir results/false_negative_three_layer_202607XX
python3 -m pytest tests/test_false_negative_three_layer_control.py -v
```

**Go/No-Go**：
- 必须实现三层 pipeline；专家审查若实际无法执行，论文需明确"protocol proposed, execution deferred to revision"。
- 若三层控制后 high-confidence negatives 占比 < 30% → 论文中明确"high false-negative risk is a known limitation"。

#### P1-09 ORD 接入

**目标**：补齐 Section 8.1 缺失的公开数据源。

**输入**：ORD 公开 protobuf 数据（`https://docs.open-reaction-database.org/`）。

**输出**：
- `chem_negative_sampling/pc_cng/build_ord.py`：ORD 解析与 normalization。
- `data/processed/ord_normalized.csv`：normalized ORD reactions。
- `data/summaries/ord_normalized_summary.json`：规模与字段统计。
- `results/ord_data_quality_audit_202607XX/`：数据质量审计。

**验收命令**：
```bash
python3 -m pc_cng.build_ord --output-dir data/processed
python3 -m pc_cng.audit_benchmark_data_quality --input data/processed/ord_normalized.csv \
  --output-dir results/ord_data_quality_audit_202607XX
python3 -m pytest tests/test_ord_build.py -v
```

**Go/No-Go**：必须接入，无门槛。若 ORD 解析失败，降级为 ORD subset（如 ORD-2024-Q3）。

#### P1-10 小规模 HTE 或 xTB/DFT 计算验证

**目标**：补齐顶刊升级版必备的"实验或计算验证"。

**输入**：高置信 synthetic negatives 子集（来自 P1-08）。

**输出**：
- `docs/computational_validation_protocol_202607XX.md`：xTB/DFT 验证协议。
- `results/xtb_dft_validation_202607XX/`：对 50–100 条 high-value synthetic negatives 的 xTB/DFT 计算结果。
- 若找到 HTE 合作：`docs/exp_hte_protocol_202607XX.md` + 实验结果。

**验收命令**：
```bash
python3 -m pc_cng.run_xtb_validation \
  --candidates results/false_negative_three_layer_202607XX/high_confidence_negatives.csv \
  --limit 100 \
  --output-dir results/xtb_dft_validation_202607XX
```

**Go/No-Go**：
- 若 xTB/DFT 支持 ≥ 60% synthetic negatives 的"低可行性"判断 → 写入论文 Section 9 E3；
- 若支持率 < 60% → 论文中明确"computational validation partial support"，并降级期刊目标。

**降级路径**：若 xTB 安装受阻，降级为 MMFF94 力场能量 + 反应势垒估算；若 DFT 完全不可行，仅做 xTB。

#### P1-11 Ni Coupling 数据缺口调研

**目标**：补齐 Section 21.6 已知的硬数据缺口。

**输入**：文献检索 + Reaxys 公开摘要（若有）。

**输出**：
- `docs/ni_coupling_data_gap_research_202607XX.md`：调研报告。
- 若找到 Ni coupling 公开数据：`data/processed/ni_coupling_supplement.csv`。

**验收命令**：
```bash
python3 -m pc_cng.research_ni_coupling_data \
  --output docs/ni_coupling_data_gap_research_202607XX.md
```

**Go/No-Go**：
- 若找到 ≥ 50 个 Ni coupling 公开反应 → 补入 benchmark；
- 否则论文中明确"Ni coupling is a known data-source limitation"。

#### P1-12 Manuscript v1 草稿（含 figures）

**目标**：基于 P1-01 至 P1-11 结果，撰写 manuscript v1。

**输入**：所有 P1 结果 + 当前 manuscript tables。

**输出**：
- `docs/manuscript_v1_202607XX.md`：完整 manuscript 草稿（含 abstract、introduction、methods、results、discussion、limitations）。
- `docs/manuscript_figures_v1_202607XX/`：Figure 1–6 草图（用 matplotlib 生成）。
- `docs/manuscript_supplementary_v1_202607XX.md`：补充材料。

**验收命令**：
```bash
python3 -m pc_cng.build_manuscript_v1 \
  --results-dir results/ \
  --output-dir docs/manuscript_v1_202607XX
python3 -m pc_cng.generate_manuscript_figures \
  --output-dir docs/manuscript_figures_v1_202607XX
```

**Go/No-Go**：
- 必须产出完整 manuscript v1，无论 P1-05 至 P1-10 是否全部通过 Go/No-Go；
- 未通过的任务结果写入 limitations 或 supplementary。

#### P1-13 项目文件结构整理（独立任务，可并行）

**目标**：解决 Section 21.2 提到的文件组织混乱。

**输入**：当前 `pc_cng_research/` 全量。

**输出**：
- 整理后的目录结构（见下文 22.3）。
- `docs/file_reorganization_manifest_202607XX.md`：记录所有移动操作的原路径 → 新路径映射，便于回溯。

**验收命令**：
```bash
bash scripts_reorganize_project_files.sh \
  --manifest-output docs/file_reorganization_manifest_202607XX.md \
  --dry-run  # 先 dry-run，确认无误后实际执行
```

**Go/No-Go**：
- 必须先 dry-run，确认所有 import / 脚本路径引用不破坏；
- 移动后必须重跑 `pytest tests/ -v` 全量测试，确保 0 失败。

### 22.2 整体验收标准

P1 阶段结束（4–6 周后）必须满足以下条件之一：

**场景 A（顶刊冲刺成功）**：
- P1-05 learned graph edit decoder 通过 Go/No-Go；
- P1-01 held-out 5k frozen MLP ≥ Chemformer + 1.0 pp 且 CI 全正；
- P1-08 三层 false-negative 控制完成；
- P1-09 ORD 接入完成；
- P1-12 manuscript v1 完成。
- → 期刊目标：Science Advances / Nature Communications / Nature Machine Intelligence。

**场景 B（强计算化学期刊）**：
- P1-05 learned decoder 至少作为 supplementary；
- P1-01 held-out 5k 结果明确（无论正负）；
- P1-08 三层 pipeline 实现但专家审查仅占位；
- P1-09 ORD 接入完成；
- P1-12 manuscript v1 完成。
- → 期刊目标：Chemical Science / Digital Discovery / JCIM / Patterns / MLST。

**场景 C（降级）**：
- P1-05 / P1-06 / P1-07 全部失败；
- P1-01 held-out 5k 仍 pending；
- P1-10 完全无计算/实验验证。
- → 期刊目标：JCIM / arXiv preprint；同时启动 P2 重新规划。

### 22.3 项目文件结构整理方案

```text
pc_cng_research/
├── README.md                              # 项目总览（新建）
├── chem_negative_sampling/
│   ├── pc_cng/                           # 主代码（保留）
│   ├── tests/                            # 测试（保留）
│   ├── evaluation/                       # 评估（保留）
│   ├── utils/                            # 工具（保留）
│   ├── scripts/                          # 新建：聚合所有 scripts_run_*.sh / scripts_setup_*.sh
│   │   ├── run_full_pipeline.sh
│   │   ├── run_external_product_prediction.sh
│   │   └── ...
│   ├── phase1_bootstrap/                 # 早期原型（保留但标注 deprecated）
│   ├── phase2_pretrain/                  # 同上
│   ├── phase3_refinement/                # 同上
│   ├── phase4_expansion/                 # 同上
│   ├── requirements.txt
│   └── README.md
├── data/                                  # 保留
│   ├── raw/
│   ├── processed/
│   ├── source_data/
│   └── summaries/
├── docs/
│   ├── 00_当前有效文档/                   # 保留：当前唯一有效文档目录
│   │   ├── 顶刊论文核心思想与从0到1落地方案.md
│   │   ├── PC-CNG-v3-项目进展汇报-20260712.md
│   │   ├── PC-CNG-v3-SOTA-gap-analysis-updated-20260711.md
│   │   └── ...（其他当前有效文档）
│   ├── 99_历史参考文档/                   # 保留：历史文档
│   ├── archive_20260719/                  # 新建：聚合当前 docs/ 根目录 38 个 .md 文件
│   │   ├── EXPERIMENT_ANALYSIS_20260710.md
│   │   ├── PC-CNG-v2-反应中心边界生成器方案.md
│   │   ├── PC-CNG-v3-OptimizationA-*.md
│   │   └── ...
│   └── backup_*/                          # 保留原位
├── external/                              # 保留
├── models/                                # 保留
├── tools/                                 # 保留
├── envs/                                  # 保留
├── outputs/                               # 保留
├── results/                               # 保留（161 个子目录，禁止删除）
│   └── archive_20260719/                  # 新建：归档 obsolete 子目录（仅创建占位，不实际移动）
├── logs/                                  # 保留
└── scripts/                               # 新建：聚合根目录 20+ 个 run_*.sh
    ├── run_v2_coslr_warm5_multiseed.sh
    ├── run_v2_filtered_baseline_multiseed.sh
    ├── run_v2_nbits8192_10seed.sh
    ├── run_v2_pairwise_margin_10seed_selected.sh
    ├── run_v2_unreacted_expanded_m3_uspto_eval.sh
    └── ...
```

**整理规则**：
1. **保留原文件**：所有 `results/` 子目录、`docs/00_当前有效文档/`、`docs/99_历史参考文档/`、`docs/backup_*/` 不动。
2. **移动根目录 .md 文件**：`docs/*.md`（38 个）移动到 `docs/archive_20260719/`，并在 `docs/README.md` 中记录原路径 → 新路径映射。
3. **移动根目录 run_*.sh**：`run_*.sh`（20 个）移动到 `scripts/`，并修复 `chem_negative_sampling/scripts_run_*.sh` 中对 `run_*.sh` 的相对路径引用。
4. **移动根目录 .py**：`evaluate_ensemble.py`、`evaluate_stacked_ensemble.py`、`summarize_full_feasibility.py` 移动到 `chem_negative_sampling/pc_cng/`。
5. **移动 chem_negative_sampling/*.md**：5 个 .md 文件移动到 `chem_negative_sampling/docs/`（新建）。
6. **deprecated 标注**：`phase1_bootstrap/` 至 `phase4_expansion/` 在各自 README.md 顶部加 `> **DEPRECATED**: This phase is superseded by pc_cng/ modules. Kept for historical reference only.`。
7. **dry-run 验证**：移动后必须重跑 `pytest chem_negative_sampling/tests/ -v` 全量测试，0 失败方可定稿。

### 22.4 trae 执行模板

P1 阶段建议按以下顺序启动 trae 会话：

```text
Session 1 (P1-00 + P1-13):
  - 锁定 P1 基线 + 整理文件结构
  - 产出：P1_initial_state_manifest_20260719.json、file_reorganization_manifest_20260719.md
  - 验证：pytest 全量通过、文件路径引用无破坏

Session 2 (P1-05 + P1-06 + P1-07):
  - 算法壁垒三件套：learned graph edit decoder、prototype calibrator、semi-hard curriculum
  - 产出：3 个新模块 + 3 个测试文件 + 3 个 10-seed 评估
  - 验证：3 个 Go/No-Go 判定

Session 3 (P1-01 + P1-02 + P1-03 + P1-04):
  - 评测四件套：held-out 5k full-beam、cross-dataset、calibration+OOD、retrosynthesis route ranking
  - 产出：4 个评估结果目录 + 主张更新
  - 验证：4 个 Go/No-Go 判定

Session 4 (P1-08 + P1-09 + P1-10 + P1-11):
  - 真实性四件套：专家审查协议、ORD 接入、xTB/DFT 验证、Ni coupling 调研
  - 产出：4 个文档/数据/结果
  - 验证：4 个 Go/No-Go 判定

Session 5 (P1-12):
  - manuscript v1 草稿 + figures
  - 产出：完整 manuscript v1 + 6 个 figures + supplementary
  - 验证：场景 A/B/C 判定
```

**trae 启动指令模板**：

```text
连接 ssh cunyuliu@36.137.135.49 -p 22，工作目录 /home/cunyuliu/pc_cng_research。
执行 P1-XX 任务（详见 docs/00_当前有效文档/顶刊论文核心思想与从0到1落地方案.md Section 22.1）。
约束：
- 禁止删除或重命名已有 results/ 子目录；
- 禁止修改 docs/00_当前有效文档/ 下既有 .md 文件（仅追加新文档）；
- 禁止占用 GPU 4（calibrate PID 2544995）；
- 所有新代码必须配套单元测试；
- 所有性能主张必须基于 10-seed paired significance test。
预期产出：[列出 P1-XX 任务输出]
验收命令：[列出 P1-XX 验收命令]
```

---

## 23. 文档版本与变更日志

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v1.0 | 2026-07-10 | 初版，Section 0–18 |
| v1.1 | 2026-07-10 | 新增 Section 19 MVP 状态 |
| v2.0 | 2026-07-19 | 新增 Section 20 实现真实状态审计；Section 21 九维自审；Section 22 P1 阶段目标（trae 执行版）；Section 23 版本日志。Section 19.4 下一步工程重点已过时，由 Section 22 取代。 |

## 24. P1 执行结果审计（2026-07-20）

### 24.1 P1 任务完成度矩阵

P1 阶段 14 个任务（P1-00 ~ P1-13）全部执行完毕，总耗时 9h27m，198 单元测试全通过。

| 任务 | 状态 | Go/No-Go | 关键数字 | 论文位置 |
|---|---|---|---|---|
| P1-00 基线锁定 | ✅ | 必做 | 28 artifact SHA-256 | Methods |
| P1-01 Held-out 5k full-beam | ✅ | **NO-GO** | MLP -10.56 pp vs Chemformer, CI [-11.60, -9.52], p<0.0001 | Limitations §1, Supp Note 1 |
| P1-02 Cross-dataset 迁移 | ✅ | **1/4 GO** | regiosqm20→uspto +1.63 pp CI [+0.59, +2.72] p=0.0028；hitea→regiosqm20 -2.69 pp 负迁移 | Main §3.4, Figure 4 |
| P1-03 Calibration + OOD | ✅ | 必做 | ECE=0.089, MCE=0.306, Brier=0.162；OOD 无显著退化 | Main §3.7, Figure 6 |
| P1-04 Retrosynthesis ranking | ✅ | **GO**（pseudo-route） | MRR +30.63 pp CI [29.23, 32.05] p<0.0001, 583/600 groups | Main §3.6, Limitations §6 |
| P1-05 GNN learned decoder | ✅ | Supplementary | 纯 PyTorch MPNN，数据同质化优势不显著 | Supp Note 2 |
| P1-06 Prototype calibrator | ✅ | **PASS** | 10 失败类型分类，p=3.2e-11 | Methods §2.2 |
| P1-07 Semi-hard curriculum | ✅ | Supplementary | +8.33 pp CI [0, 0.25]（12 groups，统计功效不足） | Supp Note 3, Limitations §2 |
| P1-08 三层 false-negative 控制 | ✅ | **GO** | 64,646→26,517 (41.02%) > 30%；Tanimoto 排除 24.3% | Main §3.5, Figure 5 |
| P1-09 ORD 接入 | ✅ | 必做 | 2,910 行真实 ORD，零重叠，严格 SMILES 有效性 47.39% | Methods §2.1, Supp Note 6 |
| P1-10 xTB/DFT 验证 | ✅ | **partial support** | 整体 48% < 60%；chemoselectivity_error 子集 66.7% GO；0/10 seeds 显著 | Limitations §3, Supp Note 4 |
| P1-11 Ni coupling 调研 | ✅ | **GO** | 1,688 反应（NiCOlit 1,665 + USPTO 6 + ORD 17） | Methods §2.1, Supp Table 6 |
| P1-12 Manuscript v1 | ✅ | 必做 | 23KB manuscript + 6 PNG figures + 11KB supplementary | — |
| P1-13 Reproducibility manifest | ✅ | 必做 | 28 artifact SHA-256 + manuscript tables | Methods |

### 24.2 主张可写入论文边界（v2，基于 P1 真实结果）

**Main claims（论文主体，已通过 10-seed paired significance）**：
1. **Cross-dataset 迁移**：PC-CNG 边界负样本从小规模 curated RegioSQM20 迁移到大规模 USPTO 时显著提升 reranking Top-1（+1.63 pp，CI 全正，p=0.0028）。**主张收窄为"小→大"方向，非普适迁移**。
2. **Retrosynthesis route ranking**：PC-CNG augmented reranker 在 pseudo-route benchmark 上 MRR +30.63 pp（CI [29.23, 32.05]，p<0.0001，583/600 groups）。**限制：baseline 为 pseudo-route，不可直接对比 AiZynthFinder 文献 MRR**。
3. **三层 false-negative 控制**：64,646 reviewed negatives → 26,517 high-confidence (41.02%) > 30% 阈值；Layer 2 Tanimoto 排除 24.3% 是主要贡献。
4. **Prototype calibrator**：10 失败类型分类 p=3.2e-11 显著，写入 Methods。
5. **Ni coupling 数据补强**：1,688 反应（NiCOlit 主导）补入 benchmark。

**Limitations（明确写入论文）**：
1. External bridge NO-GO（MLP calibrator distribution shift，-10.56 pp）
2. H3 curriculum 假设未在本数据规模下显著验证（12 groups 统计功效不足）
3. xTB/DFT partial support（整体 48%，仅 chemoselectivity_error 子集 66.7%）
4. 专家审查协议已制定但未实际执行（Layer 3 用 rule-based fallback）
5. AiZynthFinder 不可用，retrosynthesis 用 pseudo-route
6. Ni coupling 数据主要来自 NiCOlit 文献挖掘（非 USPTO 原生）
7. GNN learned decoder 未显著超越规则版（数据同质化）

### 24.3 P1 遗留问题与薄弱环节

| 编号 | 问题 | 根因 | 影响 | P2 修复方案 |
|---|---|---|---|---|
| L1 | External bridge NO-GO | MLP calibrator 未见过 chemformer beam candidates | 削弱"提升外部 SOTA"主张 | P2-04: 在 chemformer beam 上 fine-tune MLP |
| L2 | Retrosynthesis pseudo-route | AiZynthFinder 安装会降级 rdkit | 无法直接对比 SOTA MRR | P2-01: 独立 venv 装 AiZynthFinder |
| L3 | xTB/DFT partial support | xtb-python 不可用，DFT 未装 | 顶刊级独立验证缺失 | P2-02: ORCA 或 xTB 独立 venv |
| L4 | 专家审查未执行 | 无化学专家可用 | Layer 3 为 rule-based fallback | P2-03: 招募 2-3 名化学专家 |
| L5 | Cross-dataset 仅 1/4 显著 | pccng-limit=200 过小；hitea→regiosqm20 负迁移 | "普适迁移"主张不成立 | P2-05: 扩大 pccng-limit，补迁移对 |
| L6 | 无 SOTA 方法直接对比 | 仅对比 Chemformer LL | 顶刊要求多基线对比 | P2-06: 对比 Graph2SMILES/Molecular Transformer |
| L7 | GNN decoder 未超规则版 | 数据同质化（HiTEA 甲基化主导） | 架构创新主张弱 | P2-07: Transformer-based generator 消融 |
| L8 | 无反应条件下游任务 | 仅 reranking | 下游任务覆盖不足 | P2-08: condition prediction 下游 |

## 25. 九维再评估 v2（基于 P1 真实结果）

### 25.1 目标清晰度

**评分：8/10**（v1: 7/10，提升 1）

- ✅ P1 任务清单（Section 22.1）每个任务有明确输入/输出/验收命令/Go/No-Go/降级路径，14 个任务全部按规范执行
- ✅ P1 真实结果与文档预期对齐度高，无重大偏差
- ⚠️ 问题：P1 任务"广撒网"导致部分任务降级执行（P1-04 pseudo-route, P1-10 MMFF94, P1-08 专家未执行）
- **P2 建议**：聚焦"补强 L1-L8 薄弱环节"，少而精，每个任务必须有"修复哪个 P1 limitation"的明确映射

### 25.2 TODO 细致度

**评分：9/10**（v1: 8/10，提升 1）

- ✅ P1 TODO 极细致（14 任务 × 5 字段 = 70 个明确字段）
- ✅ 降级路径预声明，实际执行中触发 3 次降级均按预案执行
- ✅ 验收命令可复现（所有 CLI 命令已记录）
- **P2 建议**：保持 P1 模板，每个 P2 任务必须包含"修复 P1-LX"映射

### 25.3 SOTA 可行性

**评分：5/10**（v1: 7/10，下调 2）

- ❌ **无真正"超越 SOTA"主张**：
  - Cross-dataset +1.63 pp（仅 1/4 对显著）→ 偏弱
  - Retrosynthesis +30.63 pp → 强但 baseline 是 pseudo-route，不可直接对比 AiZynthFinder 文献 MRR
  - External bridge NO-GO → PC-CNG 外部 SOTA 主张失败
- ❌ **无 SOTA 方法直接对比**：仅对比 Chemformer LL，未对比 Graph2SMILES / Molecular Transformer / LocalRetro
- ⚠️ Calibration ECE=0.089 → 中等，未做 temperature scaling 优化
- **P2 必须补**：
  - P2-01: AiZynthFinder 真实路线 + SOTA 对比
  - P2-06: 多基线对比（Graph2SMILES, Molecular Transformer, LocalRetro）
  - P2-04: MLP calibrator 升级修复 external bridge

### 25.4 顶刊潜力

**评分：4/10**（v1: 6/10，下调 2）

- ❌ **方法学创新不足**：5 edit actions + 3-layer control 是工程组合，非根本性创新
- ❌ **无 wet-lab 验证**：顶刊（Nature Chemistry / JACS / Nature Machine Intelligence）通常要求实验验证
- ❌ **3 个核心 limitation 未解决**：external bridge NO-GO, xTB partial, 专家未执行
- ⚠️ **正向证据偏弱**：仅 2 个 main claim（cross-dataset + retrosynthesis pseudo-route）
- **期刊定位调整**：
  - **顶刊（Nature Chemistry / JACS Au / Nature Machine Intelligence）**：需 P2-01/02/03/06 全 GO，概率 < 30%
  - **强刊（J. Chem. Inf. Model. / Digital Discovery / Chem. Sci.）**：P2-01/04 GO 即可，概率 > 70%
  - **兜底（JCIM / Digital Discovery）**：当前 manuscript v1 + P2-04 GO 即可投稿
- **P2 必须补**：P2-01 AiZynthFinder 真实路线 + P2-02 DFT 验证 + P2-03 专家审查 + P2-06 SOTA 对比，四项全 GO 才有顶刊可能

### 25.5 科研故事

**评分：6/10**（v1: 7/10，下调 1）

- ✅ 故事主线"counterfactual negatives 提升化学反应预测"清晰
- ✅ 正向 + 负向结果透明披露，符合顶刊 reproducibility 趋势
- ❌ External bridge NO-GO 削弱"提升"主张
- ❌ 仅 1/4 迁移对显著，"普适迁移"故事不成立
- ❌ Retrosynthesis +30 pp 故事强但 baseline 是 pseudo-route，reviewer 会质疑
- **P2 故事重构建议**：
  - 主线 1：**"小规模 curated → 大规模 benchmark 迁移"**（聚焦 regiosqm20→uspto 显著正向）
  - 主线 2：**"retrosynthesis ranking with AiZynthFinder real routes"**（修复 pseudo-route）
  - 副线：三层 false-negative control + Ni coupling 数据补强
  - 明确限制：external bridge NO-GO 作为"future work"而非 main claim

### 25.6 数据集全面性

**评分：7/10**（v1: 6/10，提升 1）

- ✅ 5 个数据集：USPTO 530K + HiTEA 39K + RegioSQM20 2.4K + ORD 2.9K + NiCOlit 1.7K supplement
- ✅ ORD 零重叠，Ni coupling 补强 1,688 反应
- ❌ **无 Reaxys / SciFinder**（license 受限）
- ❌ **无 HTE（High-Throughput Experimentation）数据集**：顶刊通常要求
- ❌ **Ni coupling 主要来自 NiCOlit 文献**，非 USPTO 原生
- ⚠️ ORD SMILES 严格有效性仅 47.39%（fragment 扩展记号）
- **P2 补强建议**：
  - P2-09: Doyle/MacMillan metallaphotoredox HTE 公开数据集（若可获取）
  - P2-10: Reaxys 公开摘要（若 license 允许）

### 25.7 下游任务全面性

**评分：6/10**（v1: 7/10，下调 1）

- ✅ E1-E7 实验设计齐全（生成质量 / 困难度 / 假阴性 / 低数据 / 逆合成 / OOD / 消融）
- ❌ **E5 retrosynthesis 用 pseudo-route**（AiZynthFinder 不可用）
- ❌ **无反应条件预测下游**（yield prediction / condition prediction）
- ❌ **无反应类型分类下游**
- **P2 补强建议**：
  - P2-01: AiZynthFinder 真实路线（修复 E5）
  - P2-08: condition prediction 下游（USPTO condition dataset）

### 25.8 测评全面性

**评分：7/10**（v1: 7/10，持平）

- ✅ 10-seed paired significance（paired bootstrap CI + permutation p + sign-test p）
- ✅ ECE / MCE / Brier 校准指标
- ✅ OOD scaffold / template split
- ✅ 三层 false-negative 控制（ensemble + DB retrieval + rule-based）
- ❌ **MMFF94 降级**（xTB/DFT 不可用）
- ❌ **专家审查未执行**
- ❌ **无 SOTA 方法直接对比**
- ⚠️ pccng-limit=200 偏小（spec 为 1000），可能低估 PC-CNG 效果
- **P2 补强建议**：
  - P2-02: DFT 验证 chemoselectivity_error 子集
  - P2-03: 专家审查实际执行
  - P2-06: SOTA 多基线对比

### 25.9 模型架构最优性

**评分：5/10**（v1: 7/10，下调 2）

- ✅ 规则版 decoder（5 edit actions）+ MLP reranker 可解释
- ✅ Prototype calibrator PASS（p=3.2e-11）
- ❌ **GNN learned decoder 未显著超越规则版**（数据同质化）
- ❌ **主架构仍是"规则生成 + MLP reranker"**，不够"端到端可学习"
- ❌ **无 Transformer-based generator**（如 Chemformer fine-tune for negative generation）
- ⚠️ Semi-hard curriculum 未验证（H3 假设）
- **P2 补强建议**：
  - P2-07: Transformer-based generator 消融（Chemformer fine-tune）
  - 若 P2-07 显著优于规则版 → 升级为 main architecture
  - 若未显著 → 写入 ablation，保留规则版为 main

### 25.10 九维综合评分

| 维度 | v1 评分 | v2 评分 | 变化 | P2 修复优先级 |
|---|---|---|---|---|
| 目标清晰度 | 7 | 8 | +1 | 低 |
| TODO 细致度 | 8 | 9 | +1 | 低 |
| SOTA 可行性 | 7 | 5 | -2 | **高** |
| 顶刊潜力 | 6 | 4 | -2 | **高** |
| 科研故事 | 7 | 6 | -1 | 中 |
| 数据集全面性 | 6 | 7 | +1 | 中 |
| 下游任务全面性 | 7 | 6 | -1 | 中 |
| 测评全面性 | 7 | 7 | 0 | 中 |
| 模型架构最优性 | 7 | 5 | -2 | **高** |
| **综合** | **6.9** | **6.3** | **-0.6** | — |

**综合评估**：v2 综合评分 6.3/10，较 v1 下调 0.6。主要拖累项为 SOTA 可行性（-2）、顶刊潜力（-2）、模型架构最优性（-2），根因是 P1 真实结果暴露了 3 个核心 limitation（external bridge NO-GO, pseudo-route fallback, GNN 未超规则版）。

**P2 必须聚焦**：SOTA 可行性 + 顶刊潜力 + 模型架构三项，否则投稿目标需降级到 JCIM/Digital Discovery。

## 26. P2 下一阶段目标（trae 执行版）

### 26.0 P2 阶段定位

**目标**：补强 P1 遗留的 8 个薄弱环节（L1-L8），冲刺顶刊投稿或明确降级到强刊。

**优先级**：SOTA 可行性 > 顶刊潜力 > 模型架构 > 数据集全面性 > 下游任务 > 测评全面性 > 科研故事 > 目标清晰度 > TODO 细致度

**资源约束**：
- 禁止占用 GPU 4（calibrate PID 2544995 仍在运行，预计还需 8-12h）
- 可用 GPU：0, 2, 5, 6, 7
- 所有新代码必须配套单元测试
- 所有性能主张必须基于 10-seed paired significance test
- 禁止删除/重命名已有 results/ 子目录
- 禁止修改 docs/00_当前有效文档/ 下既有 .md 文件（仅追加新文档）

### 26.1 P2 任务清单

#### P2-00 P1 结果整合与 manuscript v2 基线锁定

**目标**：固化 P1 全部结果为 P2 起点。

**输入**：P1 全部结果（results/cross_dataset_transfer_20260719/, results/calibration_error_10seed_20260719/, results/ood_scaffold_template_split_20260719/, results/retrosynthesis_route_ranking_20260719/, results/false_negative_three_layer_20260719/, results/ord_data_quality_audit_20260719/, results/xtb_dft_validation_20260719/, data/processed/ni_coupling_supplement.csv, docs/manuscript_v1_20260719.md）

**输出**：
- `docs/P2_initial_state_manifest_20260720.json`：P2 起点状态（P1 全部 artifact SHA-256 + P1 测试套件状态 + manuscript v1 SHA-256）
- `docs/P2_baseline_metrics_20260720.md`：P2 基线指标（P1 main claims + limitations）

**验收命令**：
```bash
python3 -m pc_cng.build_p2_initial_manifest --output docs/P2_initial_state_manifest_20260720.json
```

**Go/No-Go**：必做，无门槛。

---

#### P2-01 AiZynthFinder 真实路线对比（修复 L2，最高优先级）

**目标**：修复 P1-04 pseudo-route fallback，用 AiZynthFinder 真实路线重跑 retrosynthesis ranking。

**输入**：USPTO-MIT-50k 真实路线数据。

**输出**：
- `chem_negative_sampling/pc_cng/run_aizynthfinder_route_ranking.py`：AiZynthFinder 真实路线 ranking CLI
- `results/aizynthfinder_route_ranking_202607XX/`：含 top-k route recall、MRR、NDCG、false-positive route rate
- `results/aizynthfinder_sota_comparison_202607XX/`：与 LocalRetro / Graph2SMILES / Molecular Transformer 对比
- 单元测试：`chem_negative_sampling/tests/test_aizynthfinder_route_ranking.py`

**实施约束**：
- **独立 venv**：AiZynthFinder 安装会降级 rdkit，必须在独立 venv（如 `/home/cunyuliu/venvs/aizynthfinder/`）安装，避免污染 pc_cng 环境
- 安装命令：`python3 -m venv /home/cunyuliu/venvs/aizynthfinder && source /home/cunyuliu/venvs/aizynthfinder/bin/activate && pip install aizynthfinder`
- 数据下载：USPTO-MIT-50k 从 `https://tdcommons.ai/single_pred_tasks/retrosyn/#uspto-50k` 或 `https://github.com/connorcoley/ochem_prediction_2` 获取

**验收命令**：
```bash
source /home/cunyuliu/venvs/aizynthfinder/bin/activate
python3 -m pc_cng.run_aizynthfinder_route_ranking \
  --routes-data data/processed/uspto_mit_50k_routes.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --output-dir results/aizynthfinder_route_ranking_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_aizynthfinder_route_ranking.py -v
```

**Go/No-Go**：
- 若 PC-CNG augmented MRR > AiZynthFinder baseline + 1.0 pp 且 CI 全正 → 写入论文 main table，修复 L2
- 若 MRR delta < 0 → 论文明确"PC-CNG negatives 不提升 AiZynthFinder 真实路线 ranking"，retrosynthesis 主张降级

**降级路径**：若 AiZynthFinder 安装仍受阻（即使独立 venv），降级为 LocalRetro + RDKit router（`pip install localretro`），或仅用 USPTO-MIT-50k ground-truth routes + heuristic ranker。

---

#### P2-02 DFT 验证 chemoselectivity_error 子集（修复 L3）

**目标**：修复 P1-10 partial support，对 chemoselectivity_error 子集做 DFT 验证。

**输入**：`results/false_negative_three_layer_20260719/high_confidence_negatives.csv` 中 `failure_type=chemoselectivity_error` 的子集（约 100-200 条）。

**输出**：
- `docs/dft_validation_protocol_202607XX.md`：DFT 验证协议（ORCA 4.x / xTB 6.5+）
- `results/dft_validation_chemoselectivity_202607XX/`：20-30 条 chemoselectivity_error 的 DFT 计算结果
- `chem_negative_sampling/pc_cng/run_dft_validation.py`：CLI 入口
- 单元测试：`chem_negative_sampling/tests/test_dft_validation.py`

**实施约束**：
- **独立 venv**：ORCA / xTB 安装在 `/home/cunyuliu/venvs/dft/`
- 优先级：ORCA 4.x (DFT) > xTB 6.5+ (半经验) > MMFF94 (力场)
- 计算 20-30 条（DFT 单条约 10-30 min，总耗时 3-15h）
- 指标：ΔG (kcal/mol), activation barrier (kcal/mol)

**验收命令**：
```bash
source /home/cunyuliu/venvs/dft/bin/activate
python3 -m pc_cng.run_dft_validation \
  --candidates results/false_negative_three_layer_20260719/high_confidence_negatives.csv \
  --failure-type chemoselectivity_error \
  --limit 30 \
  --method orca \
  --output-dir results/dft_validation_chemoselectivity_202607XX
python3 -m pytest chem_negative_sampling/tests/test_dft_validation.py -v
```

**Go/No-Go**：
- 若 DFT 支持 ≥ 60% chemoselectivity_error → 写入论文 Section 9 E3，修复 L3
- 若支持率 < 60% → 论文明确"computational validation partial support"，期刊目标降级

**降级路径**：若 ORCA 安装受阻，降级为 xTB 6.5+（`conda install -c conda-forge xtb-python`）；若 xTB 仍不可用，补做 50 条 MMFF94 chemoselectivity_error 子集（扩大 P1-10 样本量）。

---

#### P2-03 专家审查实际执行（修复 L4）

**目标**：修复 P1-08 Layer 3 rule-based fallback，实际执行专家审查协议。

**输入**：`results/expert_review_20260719/sampled_for_review.csv`（P1-08 抽样的 200-500 条）。

**输出**：
- `results/expert_review_executed_202607XX/`：审查结果
  - `reviewer_ratings_raw.csv`：每条 sample 的 5 项 Likert 评分 + free-form comment
  - `inter_annotator_agreement.json`：Cohen's κ / Fleiss' κ
  - `expert_review_summary.json`：审查通过率、失败模式分布
- 更新 `results/false_negative_three_layer_202607XX/`：用专家审查结果替换 Layer 3 rule-based fallback
- 单元测试：`chem_negative_sampling/tests/test_expert_review_execution.py`

**实施约束**：
- **需用户协助招募 2-3 名化学专家**（trae 无法自行执行）
- 若用户无法提供专家 → 明确标注"expert review protocol specified, execution deferred to revision"，论文 limitation 保留
- 审查表单：5 项 Likert 评分（chemical_validity / mechanistic_plausibility / side_product_likelihood / feasibility / overall_verdict）
- 一致性阈值：Cohen's κ ≥ 0.6（substantial agreement）

**验收命令**：
```bash
python3 -m pc_cng.execute_expert_review \
  --samples results/expert_review_20260719/sampled_for_review.csv \
  --reviewer-count 2 \
  --output-dir results/expert_review_executed_202607XX
python3 -m pytest chem_negative_sampling/tests/test_expert_review_execution.py -v
```

**Go/No-Go**：
- 若 Cohen's κ ≥ 0.6 且审查通过率 ≥ 70% → 写入论文 Section 9 E3，修复 L4
- 若 κ < 0.6 → 触发额外培训 + 复评
- 若专家无法招募 → 论文明确"protocol proposed, execution deferred to revision"

---

#### P2-04 MLP calibrator 升级修复 external bridge（修复 L1）

**目标**：修复 P1-01 NO-GO，在 chemformer beam candidates 上 fine-tune MLP calibrator。

**输入**：
- `results/external_calibration_heldout_full_beam_5k_20260719/full_candidates.csv`（59,300 candidates）
- `results/external_score_mlp_calibrator_v1_uspto12k_train_20260716/`（v1 calibrator recipe）

**输出**：
- `chem_negative_sampling/pc_cng/train_external_score_mlp_calibrator_v2.py`：v2 calibrator 训练入口（支持 chemformer beam candidates fine-tune）
- `results/external_score_mlp_calibrator_v2_chemformer_aware_202607XX/`：v2 calibrator
- `results/external_calibration_heldout_full_beam_v2_5k_202607XX/`：v2 calibrator apply 结果 + paired significance
- 单元测试：`chem_negative_sampling/tests/test_external_score_mlp_calibrator_v2.py`

**实施步骤**：
1. 从 full_beam_5k 的 59,300 candidates 中切分 train/val（80/20）
2. 在 train 上 fine-tune v1 calibrator（warm-start from `external_score_mlp_calibrator_v1_uspto12k_train_20260716`）
3. 在 val 上评估，early stopping
4. 在 held-out test 上 apply + 10-seed paired significance vs Chemformer LL baseline

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.train_external_score_mlp_calibrator_v2 \
  --candidates results/external_calibration_heldout_full_beam_5k_20260719/full_candidates.csv \
  --warm-start results/external_score_mlp_calibrator_v1_uspto12k_train_20260716 \
  --output-dir results/external_score_mlp_calibrator_v2_chemformer_aware_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pc_cng.evaluate_external_product_prediction_benchmark \
  --candidates results/external_calibration_heldout_full_beam_5k_20260719/full_candidates.csv \
  --model-dir results/external_score_mlp_calibrator_v2_chemformer_aware_202607XX \
  --output-dir results/external_calibration_heldout_full_beam_v2_5k_202607XX
python3 -m pytest chem_negative_sampling/tests/test_external_score_mlp_calibrator_v2.py -v
```

**Go/No-Go**：
- 若 v2 calibrator Top-1 ≥ Chemformer + 1.0 pp 且 CI 全正 → external bridge 升级为 main claim，修复 L1
- 若 v2 calibrator Top-1 < Chemformer → 论文明确"external bridge NO-GO confirmed"，期刊目标降级

**降级路径**：若 fine-tune 仍失败，尝试（a）ensemble 5 个独立 seed MLP；（b）temperature scaling on Chemformer LL；（c）isotonic regression calibrator。

---

#### P2-05 Cross-dataset 迁移扩大实验（修复 L5）

**目标**：修复 P1-02 仅 1/4 显著，扩大 pccng-limit + 补迁移对。

**输入**：RegioSQM20 / HiTEA / USPTO / ORD / NiCOlit 5 个数据集。

**输出**：
- `results/cross_dataset_transfer_v2_202607XX/`：10 个迁移对（5×4，去除自迁移）× 10 seeds
  - pccng-limit=1000（v1 为 200）
  - epochs=15
- 单元测试：`chem_negative_sampling/tests/test_cross_dataset_transfer_v2.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_cross_dataset_transfer_eval \
  --source regiosqm20 --target hitea \
  --pccng-limit 1000 --epochs 15 \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719 \
  --output-dir results/cross_dataset_transfer_v2_202607XX/regiosqm20_to_hitea
# 重复 10 个迁移对
python3 -m pytest chem_negative_sampling/tests/test_cross_dataset_transfer_v2.py -v
```

**Go/No-Go**：
- 若 ≥ 3/10 迁移对 CI 全正 → 写入论文 main table，修复 L5
- 若 < 3/10 → 论文明确"PC-CNG 迁移效应在小→大方向显著，其他方向不显著"

**降级路径**：若 pccng-limit=1000 生成过慢（>2h/对），降级为 pccng-limit=500。

---

#### P2-06 SOTA 多基线对比（修复 L6）

**目标**：修复无 SOTA 方法直接对比，加入 LocalRetro / Graph2SMILES / Molecular Transformer。

**输入**：USPTO-MIT-50k（标准 benchmark）。

**输出**：
- `results/sota_comparison_uspto_mit_50k_202607XX/`：4 个 SOTA 方法 × 10 seeds 对比
  - LocalRetro（`pip install localretro`）
  - Graph2SMILES（`https://github.com/coleygroup/Graph2SMILES`）
  - Molecular Transformer（`https://github.com/pschwllr/Molecular-Transformer`）
  - PC-CNG augmented reranker
- 指标：Top-1 / Top-3 / Top-5 / Top-10 / MRR / NDCG
- 单元测试：`chem_negative_sampling/tests/test_sota_comparison.py`

**实施约束**：
- **独立 venv**：`/home/cunyuliu/venvs/sota/`（避免依赖冲突）
- 每个方法独立训练 10 seeds，paired significance vs PC-CNG

**验收命令**：
```bash
source /home/cunyuliu/venvs/sota/bin/activate
python3 -m pc_cng.run_sota_comparison \
  --dataset uspto_mit_50k \
  --methods localretro,graph2smiles,molecular_transformer,pc_cng \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719 \
  --output-dir results/sota_comparison_uspto_mit_50k_202607XX
python3 -m pytest chem_negative_sampling/tests/test_sota_comparison.py -v
```

**Go/No-Go**：
- 若 PC-CNG Top-1 ≥ 3/3 SOTA + 1.0 pp 且 CI 全正 → 写入论文 main table，修复 L6
- 若 PC-CNG < 1/3 SOTA → 论文明确"PC-CNG 不超越 SOTA on USPTO-MIT-50k"，主张收窄

**降级路径**：若某 SOTA 安装受阻，跳过该方法，明确标注"X method not evaluated due to installation failure"。

---

#### P2-07 Transformer-based generator 消融（修复 L7）

**目标**：修复 GNN learned decoder 未超规则版，评估 Transformer-based generator。

**输入**：USPTO OpenMolecules + Chemformer checkpoint。

**输出**：
- `chem_negative_sampling/pc_cng/transformer_negative_generator.py`：Chemformer fine-tune for negative generation
- `results/transformer_negative_generator_202607XX/`：与规则版 + GNN 版对比的 10-seed 评估
- 单元测试：`chem_negative_sampling/tests/test_transformer_negative_generator.py`

**实施步骤**：
1. Load pretrained Chemformer（若有 checkpoint）
2. Fine-tune 生成 negative reactions（supervised on PC-CNG synthetic negatives）
3. 对比规则版 / GNN 版 / Transformer 版在 reranking Test Top-1 + 多样性 + 合法率

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.train_transformer_negative_generator \
  --train-data data/processed/uspto_openmolecules_train_only.csv \
  --val-data data/processed/regiosqm20_normalized.csv \
  --output-dir results/transformer_negative_generator_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_transformer_negative_generator.py -v
```

**Go/No-Go**：
- 若 Transformer 版 Test Top-1 ≥ 规则版 + 1.0 pp 且 CI 全正 → 升级为 main architecture，修复 L7
- 若未显著 → 写入 ablation，保留规则版为 main generator

**降级路径**：若 Chemformer fine-tune 不稳定，降级为 Graph2SMILES encoder + reaction-center decoder。

---

#### P2-08 反应条件预测下游任务（修复 L8）

**目标**：补强下游任务全面性，评估 PC-CNG negatives 对 condition prediction 的提升。

**输入**：USPTO condition prediction dataset（`https://pubs.acs.org/doi/10.1021/acscentsci.8b00357`）。

**输出**：
- `chem_negative_sampling/pc_cng/run_condition_prediction_eval.py`：CLI 入口
- `results/condition_prediction_202607XX/`：baseline vs PC-CNG augmented 对比，10-seed
- 单元测试：`chem_negative_sampling/tests/test_condition_prediction_eval.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_condition_prediction_eval \
  --dataset data/processed/uspto_condition_prediction.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --output-dir results/condition_prediction_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_condition_prediction_eval.py -v
```

**Go/No-Go**：
- 若 PC-CNG augmented Top-1 > baseline + 1.0 pp 且 CI 全正 → 写入论文 Section 9 新下游，修复 L8
- 否则作为 supplementary diagnostic

**降级路径**：若 USPTO condition dataset 不可获取，降级为 yield prediction（HiTEA yield data）。

---

#### P2-09 Manuscript v2 + 投稿准备

**目标**：基于 P2-01 ~ P2-08 结果，撰写 manuscript v2 + 投稿准备。

**输入**：P2 全部结果 + P1 manuscript v1。

**输出**：
- `docs/manuscript_v2_202607XX.md`：完整 manuscript v2
- `docs/manuscript_figures_v2_202607XX/`：6 个 Figure v2（更新 Figure 3/4/6）
- `docs/manuscript_supplementary_v2_202607XX.md`：补充材料 v2
- `docs/cover_letter_202607XX.md`：投稿 cover letter
- `docs/target_journal_decision_202607XX.md`：期刊定位决策（基于 P2 Go/No-Go 汇总）

**验收命令**：
```bash
python3 -m pc_cng.build_manuscript_v2 \
  --results-dir results/ \
  --p1-manuscript docs/manuscript_v1_20260719.md \
  --output-dir docs/manuscript_v2_202607XX
python3 -m pc_cng.generate_manuscript_figures_v2 \
  --output-dir docs/manuscript_figures_v2_202607XX
python3 -m pc_cng.generate_cover_letter \
  --target-journal auto \
  --output docs/cover_letter_202607XX.md
```

**Go/No-Go**：
- 必须产出 manuscript v2 + 期刊定位决策
- 期刊定位规则：
  - **顶刊（Nature Chemistry / JACS Au / Nature Machine Intelligence）**：P2-01/02/03/06 全 GO
  - **强刊（J. Chem. Inf. Model. / Digital Discovery / Chem. Sci.）**：P2-01/04 GO + P2-06 ≥ 1/3 SOTA
  - **兜底（JCIM / Digital Discovery）**：当前 manuscript v1 + P2-04 GO

### 26.2 整体验收标准

| 标准 | 阈值 | 验收方法 |
|---|---|---|
| P2 任务完成度 | ≥ 7/9（P2-00 ~ P2-09 中至少 7 个） | `ls results/ | grep 20260720` |
| 单元测试通过率 | 100% | `pytest chem_negative_sampling/tests/ -v` |
| Go/No-Go 判定文档化 | 9/9 任务均有判定 | `grep "Go/No-Go" docs/manuscript_v2_*.md` |
| Manuscript v2 完整性 | 8 章节 + 6 figures + supplementary | `wc -l docs/manuscript_v2_*.md` |
| 期刊定位决策 | 明确顶刊/强刊/兜底 | `cat docs/target_journal_decision_*.md` |
| Reproducibility manifest | P2 全部 artifact SHA-256 | `cat docs/P2_initial_state_manifest_*.json` |
| GPU 4 calibrate 未受干扰 | PID 2544995 仍运行 | `ps -p 2544995` |

### 26.3 trae 执行模板

**Session 1（P2-00 + P2-04 + P2-05）**：基线锁定 + 修复 L1 external bridge + 修复 L5 迁移扩大
- P2-00 必做，无门槛
- P2-04 修复 P1 最严重 NO-GO（external bridge -10.56 pp）
- P2-05 修复 P1 仅 1/4 显著（扩大 pccng-limit）

**Session 2（P2-01 + P2-06）**：SOTA 对比二件套
- P2-01 修复 L2 pseudo-route（AiZynthFinder 真实路线）
- P2-06 修复 L6 无 SOTA 对比（LocalRetro / Graph2SMILES / Molecular Transformer）
- 两者共享独立 venv `/home/cunyuliu/venvs/sota/`

**Session 3（P2-02 + P2-03）**：验证二件套
- P2-02 修复 L3 xTB partial（DFT chemoselectivity_error）
- P2-03 修复 L4 专家未执行（需用户协助招募专家）
- P2-03 若专家无法招募，明确 limitation

**Session 4（P2-07 + P2-08）**：架构与下游二件套
- P2-07 修复 L7 GNN 未超规则版（Transformer-based generator）
- P2-08 修复 L8 无条件下游（condition prediction）

**Session 5（P2-09）**：Manuscript v2 + 投稿准备
- 整合 P2-00 ~ P2-08 全部结果
- 生成 manuscript v2 + 6 figures v2 + supplementary v2
- 期刊定位决策 + cover letter

### 26.4 P2 阶段成功判据

**技术成功**（P2-01/04/06 至少 2/3 GO）：
- 修复 L1 external bridge（P2-04 GO）
- 修复 L2 pseudo-route（P2-01 GO）
- 修复 L6 无 SOTA 对比（P2-06 ≥ 1/3 SOTA GO）

**科研成功**（P2-01/04/06 全 GO + P2-02/03 至少 1 GO）：
- 顶刊投稿准备就绪
- Manuscript v2 含 4+ main claims + 3- limitations

**顶刊成功**（P2-01/02/03/06 全 GO）：
- 投稿 Nature Chemistry / JACS Au / Nature Machine Intelligence
- 4 个 SOTA 对比全 GO + DFT 验证 + 专家审查

**降级路径**：
- 若 P2-04 仍 NO-GO → 期刊目标降级到 JCIM / Digital Discovery
- 若 P2-01 无法修复（AiZynthFinder 安装失败） → 保留 pseudo-route，明确 limitation
- 若 P2-03 专家无法招募 → limitation 保留，不影响投稿

## 27. 变更日志

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v1.0 | 2026-07-10 | 初版，Section 0–18 |
| v1.1 | 2026-07-10 | 新增 Section 19 MVP 状态 |
| v2.0 | 2026-07-19 | 新增 Section 20 实现真实状态审计；Section 21 九维自审；Section 22 P1 阶段目标（trae 执行版）；Section 23 版本日志。Section 19.4 下一步工程重点已过时，由 Section 22 取代。 |
| v3.0 | 2026-07-20 | 新增 Section 24 P1 执行结果审计（14 任务全完成，198 测试通过）；Section 25 九维再评估 v2（综合评分 6.3/10，较 v1 下调 0.6，主要拖累 SOTA/顶刊/架构三项）；Section 26 P2 阶段目标（9 任务，聚焦修复 L1-L8 薄弱环节）；Section 27 变更日志 v3。Section 22 P1 任务清单已全部执行完毕，由 Section 24 结果审计取代。 |
| v4.0 | 2026-07-20 | 新增 Section 28 P2 执行结果审计（10 任务执行完毕：3 GO / 4 NO-GO / 1 deferred / 2 in-progress，611 单元测试通过）；Section 29 九维再评估 v3（综合评分 6.0/10，较 v2 再下调 0.3，主要拖累 SOTA 对比失败 + 架构革命未完成）；Section 30 P3 阶段目标（10 任务，聚焦架构革命 + SOTA 真实化 + 数据扩展 + NO-GO 修复路径）。Section 26 P2 任务清单已全部执行完毕，由 Section 28 结果审计取代。 |

---

## 28. P2 执行结果审计（2026-07-20）

### 28.1 P2 任务完成度矩阵

P2 阶段 10 个任务（P2-00 ~ P2-09）全部执行，总耗时 7h8m，611 单元测试通过（2 skipped）。**核心结果：3 GO / 4 NO-GO / 1 DEFERRED / 2 IN-PROGRESS**。

| 任务 | 状态 | Go/No-Go | 关键数字 | 论文位置 | 修复的 P1 limitation |
|---|---|---|---|---|---|
| P2-00 基线锁定 | ✅ COMPLETE | 必做 | 168KB manifest, 18 P1 子目录 SHA-256 | Methods | — |
| P2-01 AiZynthFinder 真实路线对比 | ✅ COMPLETE | **GO** | MRR 53.50% vs baseline 24.31%, Δ=+29.20 pp, CI [28.18, 30.53], p<0.0001, 144/150 groups favoured | Main §3.6, Figure 4 | ✅ 修复 L2 (pseudo-route) |
| P2-02 DFT chemoselectivity 验证 | ✅ COMPLETE | **GO** | 27/30 = 90% 支持率, mean ΔG +163,773 kcal/mol | Main §3.5, Supp Note 4 | ✅ 修复 L3 (partial support) |
| P2-03 专家审查执行 | ✅ COMPLETE (deferred) | DEFERRED | reviewer_forms 500 samples × 2 forms + protocol.md | Limitations §4 | ⚠️ 协议已制定, 执行推迟 |
| P2-04 MLP calibrator v2 (Chemformer-aware) | ✅ COMPLETE | **GO** | Top-1 55.04% vs Chemformer 52.50%, Δ=+2.54 pp, CI [1.33, 3.75], p=0.001 | Main §3.3, Figure 3 | ✅ 修复 L1 (external bridge) |
| P2-05 Cross-dataset 迁移扩大 | ⚠️ IN-PROGRESS | **NO-GO (趋势)** | 7/10 pairs 完成, 0/7 pooled CI 全正; 最佳 regiosqm20→uspto seed-CI [0.71, 1.46] pp but pooled CI [-0.003, 0.025] | Limitations §5 | ❌ L5 未修复 |
| P2-06 SOTA 多基线对比 | ✅ COMPLETE | **NO-GO** | 击败 2/3 RDKit baselines (+29.87, +29.93 pp); 输给 tanimoto_nn -45.12 pp; 3 SOTA 方法因网络不可装 | Supp Note 5, Limitations §6 | ❌ L6 未修复（部分） |
| P2-07 Transformer generator | ⚠️ IN-PROGRESS (smoke -41.50 pp) | **NO-GO (smoke)** | small PyTorch transformer from scratch, 训练 3+ 小时无 stdout 输出 | Supp Note 2, Limitations §7 | ❌ L7 未修复 |
| P2-08 反应条件预测下游 | ✅ COMPLETE | **NO-GO** | Top-1 95.49% vs baseline 97.99%, Δ=-2.50 pp, p<0.0001 (synthetic metals dataset) | Supp Note 7, Limitations §8 | ❌ L8 未修复 |
| P2-09 Manuscript v2 + 投稿准备 | ✅ COMPLETE | 必做 | 31KB manuscript + 5.9KB supplementary + 2KB cover letter + journal decision (strong tier) | — | — |

### 28.2 主张可写入论文边界（v3，基于 P2 真实结果）

**Main claims（论文主体，已通过 10-seed paired significance）**：

1. **Retrosynthesis route ranking with AiZynthFinder real routes**：PC-CNG-augmented ranker lifts MRR from 24.31% to 53.50% (Δ=+29.20 pp, CI [28.18, 30.53], p<0.0001, 144/150 groups favoured, 10-seed paired)。**P2-01 修复 L2 pseudo-route fallback**。Baseline 仍为 RDKit template router（AiZynthFinder policy models 因网络受限不可装），需在论文中明确口径。
2. **DFT chemoselectivity validation**：GFN2-xTB chemoselectivity-error subset 90% support rate (27/30 supported, mean ΔG +163,773 kcal/mol)。**P2-02 修复 L3 partial support**。
3. **External bridge v2 (Chemformer-aware calibrator)**：v2 calibrator beats Chemformer log-likelihood by 2.54 pp Top-1 (CI [1.33, 3.75], p=0.001, 10-seed paired)。**P2-04 修复 L1 external bridge NO-GO**。

**Limitations（明确写入论文，基于 P2 NO-GO 真实结果）**：

1. **Cross-dataset 迁移不普适**（P2-05 NO-GO）：7/10 pairs 中 0 个 pooled CI 全正；最佳 regiosqm20→uspto 在 seed-level 显著（CI [0.71, 1.46]）但 pooled CI 跨 0。hitea→ord 因 ord 数据集 0 groups 无法评估。**主张收窄为"小规模 curated → 大规模 benchmark 在 seed-level 显著，但 pooled 不显著"**。
2. **SOTA 对比不完整**（P2-06 NO-GO）：PC-CNG 击败 2/3 RDKit baselines（rdkit_template +29.87 pp, heuristic_validator +29.93 pp），但输给 tanimoto_nn -45.12 pp。3 个真正 SOTA 方法（LocalRetro / Graph2SMILES / Molecular Transformer）因服务器无网络访问无法安装，降级为 supplementary。**主张收窄为"PC-CNG 显著优于规则/启发式 baseline，但弱于 NN-based retrieval"**。
3. **Transformer generator 未超规则版**（P2-07 NO-GO smoke）：small PyTorch transformer from scratch 在 smoke 测试上 -41.50 pp。Full run 训练 3+ 小时无 stdout 输出（可能在 RDKit 验证密集循环中）。**主张保留规则版作为 main generator，明确"learned generator v1 negative result"**。
4. **Condition prediction 负向**（P2-08 NO-GO）：synthetic metals 数据集 Δ=-2.50 pp, p<0.0001。**降级为 supplementary diagnostic，明确"USPTO condition dataset 不可获取，synthetic dataset 不可代表真实条件分布"**。
5. **专家审查未执行**（P2-03 DEFERRED）：协议 + 表单已生成，但实际审查推迟到 revision。**Layer 3 仍为 rule-based fallback**。

### 28.3 P2 遗留问题与薄弱环节（v2，基于 P2 真实结果）

| 编号 | 问题 | 根因 | 影响 | P3 修复方案 |
|---|---|---|---|---|
| L9 (新) | 真正 SOTA 方法（LocalRetro/Graph2SMILES/Molecular Transformer）无法对比 | 服务器无外网，pip install 失败 | "超越 SOTA"主张不成立 | P3-02: 用 conda-pack 离线打包 wheel，或本地下载 source |
| L10 (新) | 主架构仍是规则 + MLP（2018 水平） | P2-07 Transformer from scratch 失败 | 顶刊级架构短板 | P3-01: 用 ChemBERTa-2 / MoLFormer / Chemformer 作为预训练 backbone |
| L11 (新) | Cross-dataset 迁移机制错误 | direct transfer 不普适 | "普适迁移"主张不成立 | P3-03: 改为 PC-CNG-aware fine-tuning，而非 direct transfer |
| L12 (新) | 反应条件数据集不真实 | synthetic metals 不可代表真实条件 | 下游任务缺失 | P3-04: USPTO 500k condition dataset / ORD conditions 真实数据 |
| L13 (新) | 真实 HTE 数据完全缺失 | 无公开 HTE 数据源接入 | 顶刊升级版必备 | P3-08: Doyle/MacMillan metallaphotoredox HTE 公开数据 |
| L14 (新) | 专家审查无法执行 | 无化学专家合作 | Layer 3 仍 rule-based | P3-07: LLM-as-judge (GPT-4/Claude) 作为专家代理 |
| L5 (强化) | cross-dataset pooled CI 全负 | pccng-limit=1000 仍不够 + direct transfer 机制错误 | 同 P2-05 | P3-03 修复 |
| L6 (强化) | SOTA 对比仅 2/3 RDKit baselines | 网络受限 | 同 P2-06 | P3-02 修复 |
| L7 (强化) | Transformer generator 训练无结果 | from scratch 无 pretraining | 同 P2-07 | P3-01 修复 |
| L8 (强化) | condition prediction 负向 | synthetic 数据 | 同 P2-08 | P3-04 修复 |
| L4 (保留) | 专家审查未执行 | 无化学专家 | 同 P2-03 | P3-07 修复 |

---

## 29. 九维再评估 v3（基于 P2 真实结果）

### 29.1 目标清晰度

**评分：8/10**（v2: 8/10，持平）

- ✅ P2 任务清单（Section 26.1）每个任务有明确输入/输出/验收命令/Go/No-Go/降级路径，10 个任务全部按规范执行
- ✅ P2 真实结果与文档预期方向对齐（3 GO 修复 L1/L2/L3）
- ⚠️ 但 P2 NO-GO 暴露新的 limitation（L9-L14），需要在 P3 阶段重新设计修复路径
- **P3 建议**：P3 任务必须明确"修复哪个 P2-LX"映射，且每个任务必须有"为何 P2 失败 + P3 如何不同"的根因分析

### 29.2 TODO 细致度

**评分：9/10**（v2: 9/10，持平）

- ✅ P2 TODO 极细致（10 任务 × 5 字段 = 50 字段）
- ✅ 降级路径预声明，实际执行中触发 4 次 NO-GO + 1 次 deferred + 2 次 in-progress 均按预案执行
- ✅ 验收命令可复现（所有 CLI 命令已记录）
- **P3 建议**：保持 P2 模板，每个 P3 任务必须包含"修复 P2-LX"映射 + "P2 失败根因" + "P3 不同方案"

### 29.3 SOTA 可行性

**评分：3/10**（v2: 5/10，下调 2，**最严重拖累项**）

- ❌ **3 个真正 SOTA 方法（LocalRetro/Graph2SMILES/Molecular Transformer）全部因网络无法安装**：服务器无外网访问，pip install 直接失败；GitHub clone 也失败
- ❌ **AiZynthFinder policy models 无法下载**：只有 RDKit template router 作为 baseline，不是真正 AiZynthFinder
- ❌ **P2-06 输给 tanimoto_nn -45.12 pp**：PC-CNG Morgan fingerprint 在 USPTO-MIT-50k 上弱于 NN-based retrieval
- ⚠️ P2-01 GO 但 baseline 是 RDKit template，不是 SOTA
- ⚠️ P2-04 GO 但只对比 Chemformer LL，未对比其他 external scorers
- **P3 必须补**：
  - P3-02: conda-pack 离线打包 SOTA 方法 wheel，或本地下载 source code 上传
  - P3-01: 用预训练 Transformer (ChemBERTa-2/MoLFormer) 替代 from-scratch 训练
  - P3-06: 加入 reaction class prediction 标准下游任务对比

### 29.4 顶刊潜力

**评分：3/10**（v2: 4/10，下调 1）

- ❌ **3 个核心 limitation 未解决**：SOTA 对比失败、Transformer 架构未落地、真实 HTE 缺失
- ❌ **方法学创新不足**：5 edit actions + 3-layer control + MLP reranker 是工程组合，非根本性创新
- ❌ **无 wet-lab 验证**：顶刊（Nature Chemistry/JACS/Nature Machine Intelligence）通常要求
- ⚠️ **3 个 GO 偏弱**：P2-01 baseline 非 SOTA, P2-02 子集小 (30 samples), P2-04 只对比 Chemformer
- **期刊定位调整**：
  - **顶刊（Nature Chemistry/JACS Au/Nature Machine Intelligence）**：需 P3-01/02/07/08 全 GO，概率 < 20%
  - **强刊（J. Chem. Inf. Model./Digital Discovery/Chem. Sci.）**：当前可投稿，P3-01 GO 升级为 strong+
  - **兜底（JCIM/Digital Discovery）**：当前 manuscript v2 即可投稿
- **P3 必须补**：P3-01 架构革命 + P3-02 SOTA 真实化 + P3-08 真实 HTE，三项全 GO 才有顶刊可能

### 29.5 科研故事

**评分：5/10**（v2: 6/10，下调 1）

- ✅ 故事主线"counterfactual negatives 提升化学反应预测"清晰
- ✅ P2-01 retrosynthesis ranking +29.20 pp 是最强故事点
- ✅ 正向 + 负向结果透明披露，符合顶刊 reproducibility 趋势
- ❌ P2-05 cross-dataset pooled CI 全负，"普适迁移"故事不成立
- ❌ P2-06 输给 tanimoto_nn，"超越 SOTA"故事不成立
- ❌ P2-07 Transformer from scratch 失败，"learned generator"故事不成立
- ❌ P2-08 condition prediction 负向，"多下游任务"故事不成立
- **P3 故事重构建议**：
  - 主线 1：**"Physics-constrained counterfactual generation as data augmentation for retrosynthesis ranking"**（聚焦 P2-01 强结果 + P3-01 架构升级）
  - 主线 2：**"DFT-validated chemoselectivity error generation"**（聚焦 P2-02 强结果 + P3-07 LLM-as-judge）
  - 副线：三层 false-negative control + Chemformer-aware calibrator
  - 明确限制：cross-dataset 不普适、SOTA 对比不完整、专家审查 deferred

### 29.6 数据集全面性

**评分：7/10**（v2: 7/10，持平）

- ✅ 5 个数据集：USPTO 530K + HiTEA 39K + RegioSQM20 2.4K + ORD 2.9K + NiCOlit 1.7K
- ✅ ORD 零重叠，Ni coupling 补强 1,688 反应
- ❌ **无 Reaxys / SciFinder / Pistachio**（license 受限）
- ❌ **无真实 HTE（High-Throughput Experimentation）数据集**：顶刊通常要求
- ❌ **Ni coupling 主要来自 NiCOlit 文献**，非 USPTO 原生
- ⚠️ ORD SMILES 严格有效性仅 47.39%（fragment 扩展记号）
- ⚠️ P2-08 用 synthetic metals dataset 不是真实条件数据
- **P3 补强建议**：
  - P3-04: USPTO 500k condition dataset 真实条件数据
  - P3-08: Doyle/MacMillan metallaphotoredox HTE 公开数据集（Doyle 2022 JACS Au, MacMillan 2022 Science）

### 29.7 下游任务全面性

**评分：4/10**（v2: 6/10，下调 2）

- ✅ E1-E7 实验设计齐全（生成质量 / 困难度 / 假阴性 / 低数据 / 逆合成 / OOD / 消融）
- ✅ Retrosynthesis route ranking GO（P2-01）
- ❌ **E5 retrosynthesis baseline 非 SOTA**（AiZynthFinder policy models 不可装）
- ❌ **Condition prediction NO-GO**（P2-08 synthetic metals）
- ❌ **无 yield regression 下游**（连续产率预测）
- ❌ **无 reaction class prediction 下游**（10-class 分类）
- ❌ **无 reward model for RL 下游**（SciAdv 2025 论文核心下游）
- ❌ **无 end-to-end forward reaction prediction**（Molecular Transformer 任务）
- **P3 补强建议**：
  - P3-04: condition prediction with USPTO 500k 真实数据
  - P3-05: yield regression with HiTEA yield data
  - P3-06: reaction class prediction 10-class
  - P3-08: HTE data 作为 in-domain 测试集

### 29.8 测评全面性

**评分：6/10**（v2: 7/10，下调 1）

- ✅ 10-seed paired significance（paired bootstrap CI + permutation p + sign-test p）
- ✅ ECE / MCE / Brier 校准指标
- ✅ OOD scaffold / template split
- ✅ 三层 false-negative 控制（ensemble + DB retrieval + rule-based）
- ✅ DFT chemoselectivity validation 90% support（P2-02 GO）
- ❌ **真正 SOTA 方法无法对比**（LocalRetro/Graph2SMILES/Molecular Transformer 网络受限）
- ❌ **专家审查未执行**（P2-03 deferred）
- ❌ **真实 HTE 测试集无**
- ⚠️ pccng-limit=1000 仍偏小（spec 为更大），P2-05 pooled CI 全负
- ⚠️ P2-07 训练无 stdout 输出，无法判断是否真的在训练
- **P3 补强建议**：
  - P3-02: conda-pack 离线安装 SOTA 方法
  - P3-07: LLM-as-judge 替代专家审查
  - P3-08: 真实 HTE 测试集

### 29.9 模型架构最优性

**评分：2/10**（v2: 5/10，下调 3，**最严重拖累项**）

- ✅ 规则版 decoder（5 edit actions）+ MLP reranker 可解释
- ✅ Prototype calibrator PASS（p=3.2e-11）
- ❌ **GNN learned decoder 未超规则版**（P1-05 supplementary，数据同质化）
- ❌ **Transformer-based generator from scratch 失败**（P2-07 NO-GO smoke -41.50 pp）
- ❌ **主架构仍是"规则生成 + MLP reranker"**（2018 水平）
- ❌ **无预训练 Transformer backbone**（ChemBERTa-2 / MoLFormer / Chemformer fine-tune 均未实现）
- ❌ **无 attention mechanism**（文档 Section 5.2 主张双通道 sequence + graph attention，实际未实现）
- ❌ **无 end-to-end 学习**（生成器和 reranker 是分离的）
- ❌ **无 multi-task learning**（reranking + yield + class 联合训练未实现）
- **P3 必须补**：
  - P3-01: **架构革命** — 用预训练 ChemBERTa-2 / MoLFormer 作为 encoder，fine-tune 端到端 generator + reranker
  - 若 P3-01 显著优于规则版 → 升级为 main architecture, 顶刊门槛达成
  - 若未显著 → 写入 ablation, 论文保留规则版为 main, 但需明确"learned generator v2 negative result"

### 29.10 九维综合评分

| 维度 | v1 评分 | v2 评分 | v3 评分 | v2→v3 变化 | P3 修复优先级 |
|---|---|---|---|---|---|
| 目标清晰度 | 7 | 8 | 8 | 0 | 低 |
| TODO 细致度 | 8 | 9 | 9 | 0 | 低 |
| SOTA 可行性 | 7 | 5 | 3 | -2 | **最高** |
| 顶刊潜力 | 6 | 4 | 3 | -1 | **最高** |
| 科研故事 | 7 | 6 | 5 | -1 | 中 |
| 数据集全面性 | 6 | 7 | 7 | 0 | 中 |
| 下游任务全面性 | 7 | 6 | 4 | -2 | **高** |
| 测评全面性 | 7 | 7 | 6 | -1 | 中 |
| 模型架构最优性 | 7 | 5 | 2 | -3 | **最高** |
| **综合** | **6.9** | **6.3** | **5.2** | **-1.1** | — |

**综合评估**：v3 综合评分 5.2/10，较 v2 再下调 1.1。主要拖累项为模型架构最优性（-3, 跌至 2/10）、SOTA 可行性（-2, 跌至 3/10）、下游任务全面性（-2, 跌至 4/10）、顶刊潜力（-1, 跌至 3/10）。根因是 P2 真实结果暴露了 6 个新 limitation（L9-L14），其中 L9（SOTA 无法对比）、L10（架构未革命）、L11（迁移机制错误）是顶刊投稿的三大障碍。

**P3 必须聚焦**：架构革命 + SOTA 真实化 + 数据扩展三项，否则投稿目标需降级到 JCIM/Digital Discovery。**当前可投稿强刊（strong tier）, 但顶刊概率 < 20%**。

---

## 30. P3 下一阶段目标（trae 执行版）

### 30.0 P3 阶段定位

**目标**：补强 P2 遗留的 6 个新薄弱环节（L9-L14），同时修复 P2 强化未解决的 5 个旧 limitation（L4-L8）。**核心战略：架构革命 + SOTA 真实化 + 数据扩展 + NO-GO 修复**。

**优先级**：架构革命 > SOTA 真实化 > 数据扩展 > 下游任务 > 测评全面性 > 科研故事 > 目标清晰度 > TODO 细致度

**资源约束**：
- 服务器：`cunyuliu@36.137.135.49`，GPU 0-7（需与现有进程共享，禁止占用 GPU 4）
- 数据目录：`/home/cunyuliu/pc_cng_research/`
- 代码目录：`/home/cunyuliu/pc_cng_research/chem_negative_sampling/pc_cng/`
- 历史产物：`results/`、`docs/` 下已有 200+ 个目录/文件，禁止删除或重命名已有 result 子目录
- 测试基线：`tests/` 下 611 个测试必须保持通过
- **GitHub repo**：`git@github.com:Cunyu-Liu/PC_CNG.git`（已建立）
- **网络受限**：服务器无外网访问，所有 SOTA 方法必须离线打包（conda-pack / wheel / source tarball）后上传

**核心创新点（顶刊门槛）**：
1. **架构革命**：从"规则生成 + MLP reranker"升级到"预训练 Transformer + 端到端学习"
2. **SOTA 真实化**：离线安装真正 SOTA 方法（LocalRetro / Graph2SMILES / Molecular Transformer）
3. **数据扩展**：接入真实 HTE 公开数据集（Doyle/MacMillan metallaphotoredox）
4. **NO-GO 修复**：cross-dataset 改为 fine-tuning 机制；condition prediction 改为真实数据；Transformer 用预训练 backbone

### 30.1 P3 任务清单

#### P3-00 P2 完整结果整合与 manuscript v2.1 锁定

**目标**：固化 P2 全部结果为 P3 起点，等待 P2-05/P2-07 完成后重新生成 manuscript v2.1。

**输入**：P2 全部结果（results/cross_dataset_transfer_v2_20260720/, results/transformer_negative_generator_20260720/, results/aizynthfinder_route_ranking_20260720/, results/dft_validation_chemoselectivity_20260720/, results/external_score_mlp_calibrator_v2_chemformer_aware_20260720/, results/sota_comparison_uspto_mit_50k_20260720/, results/condition_prediction_20260720/, results/expert_review_executed_20260720/, docs/manuscript_v2_20260720.md）

**输出**：
- `docs/P3_initial_state_manifest_202607XX.json`：P3 起点状态（P2 全部 artifact SHA-256 + P2 测试套件状态 + manuscript v2 SHA-256）
- `docs/P3_baseline_metrics_202607XX.md`：P3 基线指标（P2 main claims + limitations L4-L14）

**验收命令**：
```bash
python3 -m pc_cng.build_p3_initial_manifest --output docs/P3_initial_state_manifest_202607XX.json
# 等 P2-05/P2-07 完成后重新生成 manuscript v2.1
python3 -m pc_cng.build_manuscript_v2 \
  --results-dir results/ \
  --p1-manuscript docs/manuscript_v1_20260719.md \
  --output-dir docs
```

**Go/No-Go**：必做，无门槛。

---

#### P3-01 架构革命：预训练 Transformer 端到端 PC-CNG（最高优先级，修复 L10）

**目标**：从"规则生成 + MLP reranker"（2018 水平）升级到"预训练 Transformer + 热端到端 generator + reranker"（2025 SOTA 水平），让 Section 5.2 主张的双通道 sequence + graph attention 真正落地。

**P2-07 失败根因**：从零训练 small PyTorch transformer, 数据量不足（10K samples）, 无预训练, RDKit 验证密集导致 stdout 阻塞。

**P3-01 不同方案**：
1. **Backbone 选择**（按优先级）：
   - (a) **ChemBERTa-2**（HuggingFace, 77M 参数, SMILES 预训练）— 需离线下载 model weights
   - (b) **MoLFormer**（IBM, 23M 参数, 3D-aware）— 需离线下载
   - (c) **Chemformer**（已在 pc_cng 环境中, 50M 参数, reaction SMILES 预训练）— **推荐**: 已有, 无需下载
   - (d) 降级: 用 RoBERTa + SMILES tokenizer 从头预训练（耗时, 不推荐）
2. **架构设计**：
   - Encoder: Chemformer / ChemBERTa-2 frozen + 2-layer adapter
   - Generator: Diffusion-based graph edit decoder（替代 from-scratch Transformer）
   - Reranker: End-to-end 与 generator 联合训练（替代分离 MLP）
3. **训练策略**：
   - Stage 1: Frozen backbone + adapter tuning（10K PC-CNG negatives）
   - Stage 2: Full fine-tune with DPO 偏好优化
   - Stage 3: Multi-task learning（reranking + yield regression + class prediction）

**输入**：
- `results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv`（64,646 negatives）
- `data/processed/uspto_openmolecules_normalized.csv`（530K positives）
- Chemformer checkpoint（若已有）

**输出**：
- `chem_negative_sampling/pc_cng/transformer_e2e_pc_cng.py`：端到端 Transformer PC-CNG
- `chem_negative_sampling/pc_cng/train_transformer_e2e.py`：训练入口
- `results/transformer_e2e_pc_cng_202607XX/`：与规则版 + GNN 版 + P2-07 from-scratch 版对比的 10-seed 评估
- 单元测试：`chem_negative_sampling/tests/test_transformer_e2e_pc_cng.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.train_transformer_e2e \
  --backbone chemformer \
  --train-data results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --val-data data/processed/regiosqm20_normalized.csv \
  --output-dir results/transformer_e2e_pc_cng_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719 \
  --stage adapter --epochs 10
python3 -m pytest chem_negative_sampling/tests/test_transformer_e2e_pc_cng.py -v
```

**Go/No-Go**：
- 若 E2E Transformer 在 reranking Test Top-1 ≥ 规则版 + 1.0 pp 且 CI 全正 → 升级为 main architecture, 修复 L10, 顶刊门槛达成
- 若 E2E Transformer Test Top-1 < 规则版 → 写入 ablation, 论文保留规则版为 main, 但需明确"learned generator v2 negative result"
- 若 Chemformer backbone 不可用 → 降级为 ChemBERTa-2 + adapter

**降级路径**：
- Stage 1 adapter tuning 失败 → 尝试 LoRA tuning（更低学习率）
- LoRA 失败 → 降级为 GraphSAGE + reaction center attention（P1-05 方案升级版）
- GraphSAGE 失败 → 保留规则版, 明确 limitation

---

#### P3-02 SOTA 真实化：离线安装 + 多基线对比（修复 L9, L6 强化）

**目标**：修复 P2-06 无真正 SOTA 方法对比（LocalRetro/Graph2SMILES/Molecular Transformer 全部因网络失败）。

**P2-06 失败根因**：服务器无外网, pip install / GitHub clone 全部失败。

**P3-02 不同方案**：
1. **离线打包方案**（按优先级）：
   - (a) **conda-pack**：在本地有网络的机器上创建 conda env, `conda pack -n sota -o sota.tar.gz`, scp 上传到服务器, 解压后 `source sota/bin/activate`
   - (b) **pip download + wheel**：`pip download localretro graph2smiles molecular_transformer -d ./sota_wheels/`, scp 上传, `pip install --no-index --find-links=./sota_wheels/`
   - (c) **源码 tarball**：本地 `git clone` + `tar -czf`, scp 上传, `tar -xzf` + `pip install -e .`
2. **SOTA 方法清单**：
   - LocalRetro（`https://github.com/linmin-lin/localretro`）— 单步逆合成, atom-based
   - Graph2SMILES（`https://github.com/coleygroup/Graph2SMILES`）— graph-to-SMILES
   - Molecular Transformer（`https://github.com/pschwllr/Molecular-Transformer`）— SMILES-to-SMILES
   - Chemformer（已有）— reaction SMILES Transformer
   - Tanimoto NN（P2-06 baseline, 76% Top-1）— 作为 reference baseline
3. **Benchmark**: USPTO-MIT-50k 标准切分

**输入**：
- USPTO-MIT-50k（标准 benchmark）
- PC-CNG augmented reranker（P2-04 v2 calibrator）

**输出**：
- `/home/cunyuliu/venvs/sota_packed/`：离线打包的 SOTA 方法环境
- `chem_negative_sampling/pc_cng/run_sota_comparison_v2.py`：v2 SOTA 对比 CLI（5 方法 × 10 seeds）
- `results/sota_comparison_v2_uspto_mit_50k_202607XX/`：5 SOTA 方法 × 10 seeds 对比
- 单元测试：`chem_negative_sampling/tests/test_sota_comparison_v2.py`

**验收命令**：
```bash
# 在本地有网络的机器上
conda create -n sota_pack python=3.10
conda activate sota_pack
pip install localretro graph2smiles molecular_transformer aizynthfinder
conda pack -n sota_pack -o /tmp/sota_pack.tar.gz
scp /tmp/sota_pack.tar.gz cunyuliu@36.137.135.49:/home/cunyuliu/venvs/

# 在服务器上
mkdir -p /home/cunyuliu/venvs/sota_packed
tar -xzf /home/cunyuliu/venvs/sota_pack.tar.gz -C /home/cunyuliu/venvs/sota_packed
source /home/cunyuliu/venvs/sota_packed/bin/activate

python3 -m pc_cng.run_sota_comparison_v2 \
  --dataset uspto_mit_50k \
  --methods localretro,graph2smiles,molecular_transformer,chemformer,pc_cng \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719 \
  --output-dir results/sota_comparison_v2_uspto_mit_50k_202607XX
python3 -m pytest chem_negative_sampling/tests/test_sota_comparison_v2.py -v
```

**Go/No-Go**：
- 若 PC-CNG + P3-01 E2E Transformer Top-1 ≥ 3/4 SOTA + 1.0 pp 且 CI 全正 → 写入论文 main table, 修复 L9, 顶刊门槛达成
- 若 PC-CNG Top-1 ≥ 1/4 SOTA → 写入 supplementary, 主张收窄
- 若 PC-CNG Top-1 < 1/4 SOTA → 论文明确"PC-CNG 不超越 SOTA on USPTO-MIT-50k", 期刊目标降级到 JCIM

**降级路径**：
- conda-pack 失败 → 用 pip download wheel
- wheel 失败 → 用源码 tarball
- 全部失败 → 用 RDKit template + Tanimoto NN + Chemformer 作为 3 baselines, 明确"3 SOTA methods not evaluated due to network restriction"

---

#### P3-03 Cross-dataset 迁移机制创新：PC-CNG-aware fine-tuning（修复 L5, L11）

**目标**：修复 P2-05 cross-dataset pooled CI 全负, 从 direct transfer 改为 fine-tuning 机制。

**P2-05 失败根因**：direct transfer 假设 PC-CNG negatives 在 target dataset 上普适迁移, 但实际不同 dataset 反应族分布差异大, 直接迁移失效。

**P3-03 不同方案**：
1. **PC-CNG-aware fine-tuning**：
   - Step 1: 在 source dataset 上生成 PC-CNG negatives
   - Step 2: 在 target dataset 上 fine-tune reranker（用 source PC-CNG negatives 作为 prior, target positives 作为新数据）
   - Step 3: 对比 baseline (target-only) vs treatment (target + source PC-CNG prior)
2. **机制设计**：
   - Prior injection: source PC-CNG negatives 作为 DPO 的 reference model
   - Adapter tuning: 在 target dataset 上只训练 adapter 层
   - Elastic weight consolidation (EWC): 防止 catastrophic forgetting

**输入**：5 个数据集（RegioSQM20/HiTEA/USPTO/ORD/NiCOlit）

**输出**：
- `chem_negative_sampling/pc_cng/run_cross_dataset_finetune_v3.py`：v3 cross-dataset CLI
- `results/cross_dataset_finetune_v3_202607XX/`：10 个迁移对 × 10 seeds
- 单元测试：`chem_negative_sampling/tests/test_cross_dataset_finetune_v3.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_cross_dataset_finetune_v3 \
  --pairs regiosqm20_to_hitea,regiosqm20_to_uspto,hitea_to_uspto,hitea_to_ord,uspto_to_ord,uspto_to_nicolit,ord_to_nicolit,regiosqm20_to_ord,regiosqm20_to_nicolit,hitea_to_nicolit \
  --mechanism finetune_adapter \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719 \
  --output-dir results/cross_dataset_finetune_v3_202607XX
python3 -m pytest chem_negative_sampling/tests/test_cross_dataset_finetune_v3.py -v
```

**Go/No-Go**：
- 若 ≥ 3/10 迁移对 CI 全正 → 写入论文 main table, 修复 L5/L11
- 若 < 3/10 → 论文明确"PC-CNG-aware fine-tuning 不普适, 但 X 方向显著", 主张收窄

**降级路径**：
- finetune_adapter 失败 → 尝试 EWC
- EWC 失败 → 降级为 multi-task learning（source + target 联合训练）

---

#### P3-04 真实反应条件数据集（修复 L8, L12）

**目标**：修复 P2-08 synthetic metals 数据集不真实, 接入 USPTO 500k condition dataset。

**P2-08 失败根因**：synthetic metals dataset 不能代表真实反应条件分布。

**P3-04 不同方案**：
1. **真实 condition dataset 来源**：
   - (a) **USPTO 500k condition dataset**（`https://pubs.acs.org/doi/10.1021/acscentsci.8b00357`）— 经典 condition prediction benchmark
   - (b) **ORD conditions field**（Open Reaction Database 的 conditions 字段）— 已接入但未用于 condition prediction
   - (c) **USPTO OpenMolecules conditions**（530K reactions 的 conditions 字段）— 已接入
2. **任务设计**：
   - Catalyst prediction (10-class)
   - Solvent prediction (15-class)
   - Reagent prediction (multi-label)
   - Temperature regression
3. **Baseline**: Molecule Net baseline + Chemformer condition prediction

**输入**：USPTO OpenMolecules conditions + ORD conditions

**输出**：
- `data/processed/uspto_conditions_normalized.csv`：真实条件数据
- `chem_negative_sampling/pc_cng/run_condition_prediction_v2.py`：v2 condition prediction CLI
- `results/condition_prediction_v2_202607XX/`：baseline vs PC-CNG augmented 对比, 10-seed
- 单元测试：`chem_negative_sampling/tests/test_condition_prediction_v2.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_condition_prediction_v2 \
  --dataset data/processed/uspto_conditions_normalized.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --tasks catalyst,solvent,reagent,temperature \
  --output-dir results/condition_prediction_v2_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_condition_prediction_v2.py -v
```

**Go/No-Go**：
- 若 PC-CNG augmented 在 ≥ 2/4 tasks Top-1 > baseline + 1.0 pp 且 CI 全正 → 写入论文 Section 9 新下游, 修复 L8/L12
- 若 < 2/4 → 作为 supplementary diagnostic

**降级路径**：
- USPTO 500k 不可获取 → 用 ORD conditions（已有）
- ORD conditions 不够 → 用 HiTEA yield data（已有）

---

#### P3-05 Yield regression 下游任务（新增下游）

**目标**：补齐下游任务全面性, 实现连续产率预测。

**输入**：HiTEA yield data + USPTO yield data

**输出**：
- `chem_negative_sampling/pc_cng/run_yield_regression.py`：CLI
- `results/yield_regression_202607XX/`：baseline vs PC-CNG augmented, 10-seed
- 指标: RMSE, MAE, R²
- 单元测试：`chem_negative_sampling/tests/test_yield_regression.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_yield_regression \
  --dataset data/processed/hitea_full_normalized.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --output-dir results/yield_regression_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_yield_regression.py -v
```

**Go/No-Go**：
- 若 PC-CNG augmented RMSE < baseline - 0.05 且 CI 全正 → 写入论文 Section 9 新下游
- 否则作为 supplementary

---

#### P3-06 Reaction class prediction 下游任务（新增下游）

**目标**：补齐下游任务全面性, 实现 10-class 反应类型分类。

**输入**：USPTO OpenMolecules + 10 reaction classes（已标注）

**输出**：
- `chem_negative_sampling/pc_cng/run_reaction_class_prediction.py`：CLI
- `results/reaction_class_prediction_202607XX/`：baseline vs PC-CNG augmented, 10-seed
- 指标: Top-1 accuracy, macro-F1, confusion matrix
- 单元测试：`chem_negative_sampling/tests/test_reaction_class_prediction.py`

**验收命令**：
```bash
CUDA_VISIBLE_DEVICES=0 python3 -m pc_cng.run_reaction_class_prediction \
  --dataset data/processed/uspto_openmolecules_normalized.csv \
  --pc-cng-negatives results/uspto_openmolecules_full_generation/pc_cng_synthetic_negatives_reviewed.csv \
  --output-dir results/reaction_class_prediction_202607XX \
  --seeds 20260710,20260711,20260712,20260713,20260714,20260715,20260716,20260717,20260718,20260719
python3 -m pytest chem_negative_sampling/tests/test_reaction_class_prediction.py -v
```

**Go/No-Go**：
- 若 PC-CNG augmented macro-F1 > baseline + 1.0 pp 且 CI 全正 → 写入论文 Section 9 新下游
- 否则作为 supplementary

---

#### P3-07 LLM-as-judge 专家代理审查（修复 L4, L14）

**目标**：修复 P2-03 专家审查未执行, 用 LLM (GPT-4 / Claude) 作为化学专家代理。

**P2-03 失败根因**：无化学专家合作, 审查推迟到 revision。

**P3-07 不同方案**：
1. **LLM-as-judge 设计**：
   - 用 GPT-4 / Claude / Gemini 作为化学专家代理
   - 提供 synthetic negative + 5 项 Likert 评分提示
   - 每条 sample 用 3 个不同 LLM 投票, majority vote
2. **审查协议**：
   - 500 条高置信 synthetic negatives（来自 P1-08 三层控制后）
   - 5 项 Likert: chemical_validity / mechanistic_plausibility / side_product_likelihood / feasibility / overall_verdict
   - 一致性: 3-LLM agreement rate + Cohen's κ
3. **与传统专家对比**：若有 1-2 名化学专家审查小样本 (50 条), 计算 LLM vs human agreement

**输入**：`results/false_negative_three_layer_20260719/high_confidence_negatives.csv`

**输出**：
- `chem_negative_sampling/pc_cng/run_llm_judge_review.py`：CLI
- `results/llm_judge_review_202607XX/`：3-LLM 投票结果 + 一致性
- 单元测试：`chem_negative_sampling/tests/test_llm_judge_review.py`

**验收命令**：
```bash
python3 -m pc_cng.run_llm_judge_review \
  --samples results/false_negative_three_layer_20260719/high_confidence_negatives.csv \
  --llms gpt4,claude,gemini \
  --limit 500 \
  --output-dir results/llm_judge_review_202607XX
python3 -m pytest chem_negative_sampling/tests/test_llm_judge_review.py -v
```

**Go/No-Go**：
- 若 3-LLM agreement ≥ 70% 且 Cohen's κ ≥ 0.6 → 写入论文 Section 9 E3, 修复 L4/L14
- 若 agreement < 70% → 论文明确"LLM-as-judge moderate agreement, execution deferred to revision"

**降级路径**：
- GPT-4 API 不可用 → 用 Claude / Gemini
- 全部 LLM API 不可用 → 降级为 RDKit-based reaction class prediction + aromaticity/valence check

---

#### P3-08 真实 HTE 数据获取（修复 L13, 顶刊门槛）

**目标**：接入真实 HTE (High-Throughput Experimentation) 公开数据集, 补齐顶刊升级版必备。

**数据源**（按优先级）：
1. **Doyle 2022 JACS Au metallaphotoredox HTE**（`https://github.com/DoyleLab/...`）— 公开, 1,760 reactions
2. **MacMillan 2022 Science metallaphotoredox HTE**（若可获取）
3. **Doyle C–N coupling HTE**（`https://github.com/DoyleLab/dreidels`）— 4,608 reactions
4. **Merck HTE data**（若可获取）

**输入**：公开 HTE 数据集

**输出**：
- `data/processed/hte_doyle_2022.csv`：normalized HTE data
- `data/processed/hte_merck_cn_coupling.csv`：normalized Merck C-N coupling HTE
- `results/hte_data_audit_202607XX/`：数据质量审计
- 单元测试：`chem_negative_sampling/tests/test_hte_data_build.py`

**验收命令**：
```bash
python3 -m pc_cng.build_hte_data --output-dir data/processed
python3 -m pc_cng.audit_benchmark_data_quality \
  --input data/processed/hte_doyle_2022.csv \
  --output-dir results/hte_data_audit_202607XX
python3 -m pytest chem_negative_sampling/tests/test_hte_data_build.py -v
```

**Go/No-Go**：
- 若接入 ≥ 1 个 HTE dataset → 写入论文 Section 8 数据路线, 顶刊门槛 +1
- 若无法获取 → 论文明确"HTE data not publicly available", 期刊目标降级

**降级路径**：若所有 HTE 公开数据不可获取 → 用 USPTO 低产率 subset 作为 pseudo-HTE

---

#### P3-09 Manuscript v3 + 顶刊冲刺投稿准备

**目标**：基于 P3-01 ~ P3-08 结果, 撰写 manuscript v3 + 顶刊冲刺投稿准备。

**输入**：P3 全部结果 + P2 manuscript v2

**输出**：
- `docs/manuscript_v3_202607XX.md`：完整 manuscript v3
- `docs/manuscript_figures_v3_202607XX/`：6 个 Figure v3（更新 Figure 1/2/3/4/6）
- `docs/manuscript_supplementary_v3_202607XX.md`：补充材料 v3
- `docs/cover_letter_v3_202607XX.md`：投稿 cover letter
- `docs/target_journal_decision_v3_202607XX.md`：期刊定位决策（基于 P3 Go/No-Go 汇总）

**验收命令**：
```bash
python3 -m pc_cng.build_manuscript_v3 \
  --results-dir results/ \
  --p2-manuscript docs/manuscript_v2_20260720.md \
  --output-dir docs
python3 -m pc_cng.generate_manuscript_figures_v3 \
  --output-dir docs/manuscript_figures_v3_202607XX
python3 -m pc_cng.generate_cover_letter \
  --target-journal auto \
  --output docs/cover_letter_v3_202607XX.md
```

**Go/No-Go**：
- 必须产出 manuscript v3 + 期刊定位决策
- 期刊定位规则：
  - **顶刊（Nature Chemistry/JACS Au/Nature Machine Intelligence）**：P3-01/02/07/08 全 GO
  - **强刊（J. Chem. Inf. Model./Digital Discovery/Chem. Sci.）**：P3-01/04 GO + P3-02 ≥ 1/4 SOTA
  - **兜底（JCIM/Digital Discovery）**：当前 manuscript v2 + P3-04 GO

### 30.2 整体验收标准

| 标准 | 阈值 | 验收方法 |
|---|---|---|
| P3 任务完成度 | ≥ 7/9（P3-00 ~ P3-09 中至少 7 个） | `ls results/ \| grep 202607XX` |
| 单元测试通过率 | 100% | `pytest chem_negative_sampling/tests/ -v` |
| Go/No-Go 判定文档化 | 9/9 任务均有判定 | `grep "Go/No-Go" docs/manuscript_v3_*.md` |
| Manuscript v3 完整性 | 8 章节 + 6 figures + supplementary | `wc -l docs/manuscript_v3_*.md` |
| 期刊定位决策 | 明确顶刊/强刊/兜底 | `cat docs/target_journal_decision_v3_*.md` |
| Reproducibility manifest | P3 全部 artifact SHA-256 | `cat docs/P3_initial_state_manifest_*.json` |
| GitHub repo 推送 | 代码 + 数据 + 文档全量推送 | `git log --oneline` |

### 30.3 trae 执行模板

**Session 1（P3-00 + P3-01）**：基线锁定 + 架构革命
- P3-00 必做, 无门槛（需等 P2-05/P2-07 完成后重新生成 manuscript v2.1）
- P3-01 架构革命（最高优先级, 顶刊门槛）
- 产出: E2E Transformer PC-CNG + 10-seed 评估

**Session 2（P3-02）**：SOTA 真实化
- conda-pack 离线打包 SOTA 方法
- 5 方法 × 10 seeds 对比
- 产出: SOTA 对比 main table

**Session 3（P3-03 + P3-04）**：迁移机制 + 真实条件数据
- P3-03 cross-dataset fine-tuning 机制
- P3-04 真实反应条件数据集
- 产出: 2 个新 main claims

**Session 4（P3-05 + P3-06 + P3-07）**：下游三件套 + LLM judge
- P3-05 yield regression
- P3-06 reaction class prediction
- P3-07 LLM-as-judge 专家代理
- 产出: 3 个新下游任务 + 专家审查协议

**Session 5（P3-08 + P3-09）**：HTE + manuscript v3
- P3-08 真实 HTE 数据获取
- P3-09 manuscript v3 + 投稿准备
- 产出: 顶刊冲刺准备就绪

### 30.4 P3 阶段成功判据

**技术成功**（P3-01/02/04 至少 2/3 GO）：
- 修复 L10 架构革命（P3-01 GO）
- 修复 L9 SOTA 真实化（P3-02 ≥ 1/4 SOTA GO）
- 修复 L8/L12 真实条件数据（P3-04 GO）

**科研成功**（P3-01/02/04/07/08 至少 3/5 GO）：
- 顶刊投稿准备就绪
- Manuscript v3 含 5+ main claims + 3- limitations

**顶刊成功**（P3-01/02/07/08 全 GO）：
- 投稿 Nature Chemistry / JACS Au / Nature Machine Intelligence
- E2E Transformer + SOTA 对比 + LLM judge + 真实 HTE 全部 GO

**降级路径**：
- 若 P3-01 仍 NO-GO → 期刊目标降级到 JCIM / Digital Discovery
- 若 P3-02 SOTA 全部安装失败 → 保留 P2-06 RDKit baseline 对比, 明确 limitation
- 若 P3-08 HTE 数据无法获取 → 期刊目标降级到强刊, 明确 limitation

---

## 31. GitHub 仓库结构（P3 阶段新加）

P3 阶段开始将项目代码同步到 GitHub `git@github.com:Cunyu-Liu/PC_CNG.git`, 便于复现与社区反馈。

### 31.1 仓库结构

```text
PC_CNG/
├── README.md                              # 项目总览（含 abstract, install, usage）
├── LICENSE                                # MIT
├── .gitignore                            # 排除 results/ 大文件、data/raw/、logs/
├── requirements.txt                       # 依赖清单
├── chem_negative_sampling/
│   ├── pc_cng/                           # 主代码（已存在）
│   ├── tests/                            # 测试（已存在, 611 测试）
│   └── ...
├── data/
│   ├── processed/                        # 仅保留 normalized 小文件（< 100 MB）
│   └── raw/                               # .gitignore 排除
├── docs/
│   ├── 00_当前有效文档/                   # 当前唯一有效文档目录
│   │   └── 顶刊论文核心思想与从0到1落地方案.md
│   ├── manuscript_v2_20260720.md         # manuscript v2
│   ├── P2_initial_state_manifest_*.json  # P2 manifest
│   └── ...
├── results/                               # .gitignore 排除大目录（仅保留 manifest）
│   └── README.md                          # 说明如何重新生成
└── scripts/                               # 运行脚本
```

### 31.2 推送策略

1. **首次推送**：代码 + 文档 + 小数据 + manifest（不包含 results/ 大文件）
2. **后续推送**：每次 P3 任务完成后推送代码 + 文档更新
3. **release tag**：P3 阶段结束打 v3.0 tag

### 31.3 验收命令

```bash
# 在服务器上初始化 git
cd /home/cunyuliu/pc_cng_research
git init
git remote add origin git@github.com:Cunyu-Liu/PC_CNG.git
git add chem_negative_sampling/ docs/ data/processed/ scripts/ README.md LICENSE .gitignore requirements.txt
git commit -m "Initial commit: PC-CNG v3 + P1/P2 results"
git push -u origin main

# 后续推送
git add docs/ chem_negative_sampling/
git commit -m "P3-XX: <task description>"
git push origin main
```
